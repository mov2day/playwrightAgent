---
phase: 06-stabilization-and-v1-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/localToolRunner.ts
  - src/adapters/auditFileSink.ts
  - src/participant/handler.ts
  - src/pipeline/orchestrator.ts
  - tests/unit/redaction-patterns.test.ts
  - tests/integration/security-boundary-local-tool-only.test.ts
  - tests/integration/security-leak-canary.test.ts
  - tests/integration/audit-redaction-persistence.test.ts
autonomous: true
requirements: [SECU-01, SECU-02]
user_setup: []
must_haves:
  truths:
    - Jira/Confluence credential handling remains local-tool/env-only and never enters model-facing payload structures.
    - Leak-canary secret strings are redacted before persistence and never appear unredacted in event/audit output.
    - Secret-leak checks fail closed: any unredacted canary in model-bound or persisted payload paths fails verification.
  artifacts:
    - src/adapters/localToolRunner.ts enforces centralized secret redaction patterns used by local-tool and audit paths.
    - src/adapters/auditFileSink.ts persists only redacted payloads with deterministic redaction evidence metadata.
    - tests/integration/security-boundary-local-tool-only.test.ts and tests/integration/security-leak-canary.test.ts assert SECU-01/02 boundaries.
  key_links:
    - local-tool command invocation -> sanitized stdout/stderr/error -> downstream prompt/event/audit payload safety.
    - pipeline emit payloads -> audit sink serialization -> persisted NDJSON records with redaction evidence.
    - leak-canary fixture injection -> integration assertions -> fail-closed release blocker signal.
---

<objective>
Implement and prove Phase 6 security boundaries for credential isolation and no-secret leakage.

Purpose: Satisfy SECU-01 and SECU-02 with deterministic regression coverage and fail-closed verification behavior.
Output: Hardened redaction/boundary handling plus dedicated leak-canary integration tests.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-stabilization-and-v1-readiness/06-CONTEXT.md
@.planning/phases/06-stabilization-and-v1-readiness/06-RESEARCH.md
@.planning/phases/06-stabilization-and-v1-readiness/06-PATTERNS.md
@.planning/phases/06-stabilization-and-v1-readiness/06-VALIDATION.md
@src/adapters/localToolRunner.ts
@src/adapters/auditFileSink.ts
@src/participant/handler.ts
@src/pipeline/orchestrator.ts
@tests/integration/audit-redaction-persistence.test.ts

<interfaces>
From `src/adapters/localToolRunner.ts`:
```ts
export function redactSensitiveText(value: string): string;
```

From `src/adapters/auditFileSink.ts`:
```ts
export interface PersistedAuditRecord extends PipelineEvent {
  schemaVersion: string;
  persistedAt: string;
  redactionEvidence: {
    redacted: true;
    fieldCount: number;
    appliedRules: string[];
  };
}
```

From `src/adapters/eventSink.ts`:
```ts
export interface PipelineEvent {
  requestId: string;
  stage: string;
  action: string;
  timestamp: string;
  details?: Record<string, unknown>;
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-tool execution -> event/prompt/audit payloads | Secrets in command output or thrown errors can leak into model-facing or persisted payload fields |
| participant/orchestrator details -> durable audit file | Unredacted secret text in details fields becomes long-lived disclosure risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | `src/adapters/localToolRunner.ts` | mitigate | Expand and normalize redaction patterns for bearer/auth/token/api-key variants before any downstream consumption |
| T-06-02 | I | `src/adapters/auditFileSink.ts` | mitigate | Preserve recursive pre-persist redaction and assert no raw canary survives in persisted NDJSON records |
| T-06-03 | R | participant/orchestrator event emit paths | mitigate | Add leak-canary integration assertions that event details are never stored unredacted |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Harden shared redaction pattern coverage and prove deterministic behavior</name>
  <files>src/adapters/localToolRunner.ts, tests/unit/redaction-patterns.test.ts, tests/integration/audit-redaction-persistence.test.ts</files>
  <read_first>src/adapters/localToolRunner.ts, src/adapters/auditFileSink.ts, tests/integration/audit-redaction-persistence.test.ts, .planning/phases/06-stabilization-and-v1-readiness/06-CONTEXT.md, .planning/phases/06-stabilization-and-v1-readiness/06-PATTERNS.md</read_first>
  <behavior>
    - Redaction helper covers bearer tokens, authorization pairs, and key/token/secret style values (including header-style forms).
    - Audit persistence tests verify redaction outcomes and deterministic evidence metadata.
  </behavior>
  <acceptance_criteria>
    - `rg "Bearer|authorization|api[_-]?key|token|secret" src/adapters/localToolRunner.ts` returns 1+ matches
    - `rg "redactionEvidence|appliedRules|fieldCount" src/adapters/auditFileSink.ts tests/integration/audit-redaction-persistence.test.ts` returns 1+ matches
    - `npm run test -- tests/unit/redaction-patterns.test.ts tests/integration/audit-redaction-persistence.test.ts` exits 0
  </acceptance_criteria>
  <action>Update `redactSensitiveText` in `src/adapters/localToolRunner.ts` to normalize additional secret formats needed for Phase 6 leak-canary checks (including header-like and key/value token variants) while preserving existing behavior. Add `tests/unit/redaction-patterns.test.ts` with deterministic fixtures that include bearer, authorization, token, secret, and api-key variants. Extend `tests/integration/audit-redaction-persistence.test.ts` fixtures so persisted records assert no raw canary tokens survive and that `redactionEvidence.appliedRules` remains populated.</action>
  <verify>
    <automated>npm run test -- tests/unit/redaction-patterns.test.ts tests/integration/audit-redaction-persistence.test.ts</automated>
  </verify>
  <done>Shared redaction path is stricter, test-backed, and deterministic for all phase-6 canary formats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SECU-01 credential-boundary regression tests for local-tool adapters</name>
  <files>tests/integration/security-boundary-local-tool-only.test.ts, src/participant/handler.ts, src/pipeline/orchestrator.ts</files>
  <read_first>src/adapters/jiraClient.ts, src/adapters/confluenceClient.ts, src/participant/handler.ts, src/pipeline/orchestrator.ts, tests/integration/no-ticket-flow.test.ts, .planning/phases/06-stabilization-and-v1-readiness/06-RESEARCH.md</read_first>
  <behavior>
    - Adapter command-arg payloads contain only request and query/ticket data, never credential strings.
    - Participant/orchestrator event details do not serialize raw environment secret values.
  </behavior>
  <acceptance_criteria>
    - `rg "scripts/jira-fetch\\.mjs|scripts/confluence-search\\.mjs" src/adapters/jiraClient.ts src/adapters/confluenceClient.ts` returns 1+ matches
    - `rg "interactionType|decisionAction|details" src/participant/handler.ts src/pipeline/orchestrator.ts` returns 1+ matches
    - `npm run test -- tests/integration/security-boundary-local-tool-only.test.ts` exits 0
  </acceptance_criteria>
  <action>Create `tests/integration/security-boundary-local-tool-only.test.ts` that injects stub command runners into Jira/Confluence clients, captures executed command/args, and asserts no credential-like values are passed in arguments or surfaced in returned error text. If test reveals unsanitized participant/orchestrator event details, patch those emit paths to sanitize detail payload strings before emission so model/audit surfaces remain free of raw secret text.</action>
  <verify>
    <automated>npm run test -- tests/integration/security-boundary-local-tool-only.test.ts</automated>
  </verify>
  <done>Credential boundary assumptions are enforced by automated regression tests and guarded in event emission paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add fail-closed leak-canary integration checks across event and audit persistence paths</name>
  <files>tests/integration/security-leak-canary.test.ts, src/adapters/auditFileSink.ts, src/participant/handler.ts, src/pipeline/orchestrator.ts</files>
  <read_first>tests/integration/audit-persistence-request-correlation.test.ts, tests/integration/audit-redaction-persistence.test.ts, src/adapters/auditFileSink.ts, src/participant/handler.ts, src/pipeline/orchestrator.ts, .planning/phases/06-stabilization-and-v1-readiness/06-CONTEXT.md</read_first>
  <behavior>
    - Canary values injected into representative prompt/event detail payloads are redacted before persistence.
    - Tests fail immediately when unredacted canary tokens are detected in persisted NDJSON output.
  </behavior>
  <acceptance_criteria>
    - `rg "LEAK_CANARY|REDACTED|not\\.toContain" tests/integration/security-leak-canary.test.ts` returns 1+ matches
    - `rg "redactionEvidence|requestId|schemaVersion" tests/integration/security-leak-canary.test.ts` returns 1+ matches
    - `npm run test -- tests/integration/security-leak-canary.test.ts` exits 0
  </acceptance_criteria>
  <action>Add `tests/integration/security-leak-canary.test.ts` using deterministic request IDs and fixed timestamps. Emit events containing known canary values (bearer, authorization, api-key, token) through the same event/audit path used in runtime and assert persisted files contain `[REDACTED]` markers and never raw canary strings. Ensure this test is fail-closed by asserting exact absence of raw canaries in serialized persisted content.</action>
  <verify>
    <automated>npm run test -- tests/integration/security-leak-canary.test.ts</automated>
  </verify>
  <done>Phase 6 has explicit no-leak proofs across event and persistence surfaces with fail-closed checks.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/redaction-patterns.test.ts tests/integration/security-boundary-local-tool-only.test.ts tests/integration/security-leak-canary.test.ts tests/integration/audit-redaction-persistence.test.ts
</verification>

<success_criteria>
- SECU-01 satisfied: local-tool adapter credential boundary is test-backed and fail-closed.
- SECU-02 satisfied: leak-canary strings are provably absent from model-bound/event/audit persisted payload surfaces.
- Threats T-06-01/T-06-02/T-06-03 mitigated with deterministic redaction + integration evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-stabilization-and-v1-readiness/06-01-SUMMARY.md`
</output>
