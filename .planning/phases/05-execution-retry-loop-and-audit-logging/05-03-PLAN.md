---
phase: 05-execution-retry-loop-and-audit-logging
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/adapters/auditFileSink.ts
  - src/adapters/eventSink.ts
  - src/pipeline/events.ts
  - src/pipeline/orchestrator.ts
  - src/participant/handler.ts
  - tests/integration/audit-persistence-request-correlation.test.ts
  - tests/integration/audit-redaction-persistence.test.ts
  - tests/integration/request-correlation.test.ts
autonomous: true
requirements: [SECU-03, SECU-04]
user_setup: []
must_haves:
  truths:
    - All AI interactions and gate decisions are durably persisted with request correlation fields.
    - Persistent audit records are redacted before write and include deterministic redaction evidence metadata.
    - Live UX event behavior remains intact via in-memory sink while file sink persists in parallel.
  artifacts:
    - src/adapters/auditFileSink.ts persists request-scoped redacted NDJSON under `.planning/logs/audit/`.
    - src/adapters/eventSink.ts composes in-memory + file sinks as default dual-sink runtime behavior.
    - src/pipeline/events.ts + src/pipeline/orchestrator.ts + src/participant/handler.ts emit enriched correlation fields for audit replay.
  key_links:
    - `createPipelineEvent` envelope -> composite sink fan-out -> in-memory + persistent sinks.
    - `redactSensitiveText` -> audit serializer -> `.planning/logs/audit/<requestId>.ndjson`.
    - guardrail/approval/run decisions -> event details -> persistent records with correlation metadata.
---

<objective>
Implement dual-sink audit logging with pre-persistence redaction and deterministic trace metadata.

Purpose: Satisfy SECU-03/SECU-04 by making all AI interactions + gate decisions queryable and safe for forensic review.
Output: Persistent audit sink, sink composition wiring, enriched event envelope fields, and integration tests for correlation/redaction guarantees.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-RESEARCH.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-PATTERNS.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-02-PLAN.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-04-SUMMARY.md
@src/adapters/localToolRunner.ts
@src/adapters/eventSink.ts
@src/pipeline/events.ts

<interfaces>
From `src/adapters/eventSink.ts`:
```ts
export interface PipelineEvent {
  requestId: string;
  stage: string;
  action: string;
  timestamp: string;
  confidenceProfileId?: string;
  decisionGate?: 'reject' | 'approval_required' | 'continue';
  details?: Record<string, unknown>;
}

export interface EventSink {
  emit(event: PipelineEvent): void;
}
```

From `src/pipeline/events.ts`:
```ts
export interface PipelineStageEvent {
  requestId: string;
  stage: PipelineStage;
  action: string;
  state?: PipelineState;
  timestamp: string;
  details?: Record<string, unknown>;
}
```

From `src/adapters/localToolRunner.ts`:
```ts
export function redactSensitiveText(value: string): string;
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime event stream -> durable storage | Missing or malformed persistence breaks audit completeness |
| raw event details -> persisted log file | Unredacted tokens/secrets create high-severity disclosure risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | I | `auditFileSink.ts` | mitigate | Apply shared redaction function before serialization and store `redactionEvidence` markers per record (D-12, SECU-04) |
| T-05-08 | R | `eventSink.ts` + `events.ts` | mitigate | Persist canonical request-correlated envelope (`requestId`, stage, action, timestamp, decision metadata) for all AI/gate interactions (D-13, SECU-03) |
| T-05-09 | D | dual sink fan-out | mitigate | Keep in-memory sink non-blocking and isolate file sink write errors to explicit audit error events without dropping runtime UX stream |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add persistent redacted audit file sink keyed by requestId</name>
  <files>src/adapters/auditFileSink.ts, tests/integration/audit-redaction-persistence.test.ts</files>
  <read_first>src/adapters/auditFileSink.ts, tests/integration/audit-redaction-persistence.test.ts, src/adapters/localToolRunner.ts, src/adapters/eventSink.ts, src/pipeline/writer/surgicalWriter.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md, .planning/phases/05-execution-retry-loop-and-audit-logging/05-PATTERNS.md</read_first>
  <behavior>
    - Records are persisted under `.planning/logs/audit/<requestId>.ndjson`.
    - Redaction runs before every append write.
    - Each record includes deterministic redaction evidence metadata.
  </behavior>
  <acceptance_criteria>
    - `rg "\\.planning/logs/audit|requestId|ndjson" src/adapters/auditFileSink.ts` returns 1+ matches (D-11)
    - `rg "redactSensitiveText|appendFile|redactionEvidence|schemaVersion" src/adapters/auditFileSink.ts` returns 1+ matches (D-12)
    - `rg "retentionDays|maxFileBytes|rotateAuditFile" src/adapters/auditFileSink.ts` returns 1+ matches (discretion: rotation policy)
    - `npm run test -- tests/integration/audit-redaction-persistence.test.ts` exits 0
  </acceptance_criteria>
  <action>Create `AuditFileSink` adapter that serializes one NDJSON line per event into `.planning/logs/audit/<requestId>.ndjson` (D-11). Apply PATTERNS analog `src/pipeline/writer/surgicalWriter.ts` file-I/O safety steps (`ensureParentDirectory` before write, deterministic path handling, bounded append/write flow) while keeping `EventSink` contract semantics. Redact all serializable event fields before persistence using `redactSensitiveText` and persist deterministic `redactionEvidence` metadata (`appliedRules`, `fieldCount`, `redacted=true`) per record (D-12). Define explicit rotation defaults in code (`retentionDays=14`, `maxFileBytes=5_000_000`) and implement `rotateAuditFile` guard so long-running requests remain bounded.</action>
  <verify>
    <automated>npm run test -- tests/integration/audit-redaction-persistence.test.ts</automated>
  </verify>
  <done>Persistent sink writes request-scoped redacted records with traceable redaction evidence and bounded rotation policy defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose default dual sink and wire participant/orchestrator to emit to both sinks</name>
  <files>src/adapters/eventSink.ts, src/participant/handler.ts, src/pipeline/orchestrator.ts, tests/integration/request-correlation.test.ts</files>
  <read_first>src/adapters/eventSink.ts, src/participant/handler.ts, src/pipeline/orchestrator.ts, tests/integration/request-correlation.test.ts, src/pipeline/events.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md</read_first>
  <behavior>
    - Runtime sink emits to in-memory and persistent sinks in same event path.
    - Existing live event consumption behavior remains unchanged.
    - Default sink creation path uses dual sink unless explicit sink override is injected.
  </behavior>
  <acceptance_criteria>
    - `rg "CompositeEventSink|createDefaultEventSink|AuditFileSink|InMemoryEventSink" src/adapters/eventSink.ts` returns 1+ matches (D-10)
    - `rg "deps\\.eventSink \\?\\? createDefaultEventSink" src/participant/handler.ts` returns 1+ matches (D-10)
    - `rg "eventSink\\.emit\\(" src/pipeline/orchestrator.ts` returns 1+ matches
    - `npm run test -- tests/integration/request-correlation.test.ts` exits 0
  </acceptance_criteria>
  <action>Implement composite sink fan-out with in-memory + file sinks as default runtime behavior (D-10). Update participant/orchestrator default sink wiring to use `createDefaultEventSink()` when no injected sink is provided, preserving existing request-correlation event emission semantics while adding persistence.</action>
  <verify>
    <automated>npm run test -- tests/integration/request-correlation.test.ts</automated>
  </verify>
  <done>Dual-sink behavior is default-on and backward-compatible with existing in-memory event expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enrich audit envelope for AI interaction and gate-decision traceability</name>
  <files>src/pipeline/events.ts, src/pipeline/orchestrator.ts, src/participant/handler.ts, tests/integration/audit-persistence-request-correlation.test.ts</files>
  <read_first>src/pipeline/events.ts, src/pipeline/orchestrator.ts, src/participant/handler.ts, tests/integration/audit-persistence-request-correlation.test.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md, .planning/phases/02-context-ingestion-and-confidence-engine/02-04-SUMMARY.md</read_first>
  <behavior>
    - Audit payload includes request-correlation fields for AI interactions and gate decisions.
    - Decision records include action, gate type, and operator comment metadata.
    - Event schema remains deterministic and versioned for replay.
  </behavior>
  <acceptance_criteria>
    - `rg "schemaVersion|interactionType|decisionAction|decisionComment" src/pipeline/events.ts` returns 1+ matches (D-13)
    - `rg "ai_interaction|gate_decision|guardrail_decision_recorded" src/pipeline/orchestrator.ts src/participant/handler.ts` returns 1+ matches (SECU-03)
    - `rg "requestId|timestamp|stage|action" tests/integration/audit-persistence-request-correlation.test.ts` returns 1+ matches
    - `npm run test -- tests/integration/audit-persistence-request-correlation.test.ts tests/integration/audit-redaction-persistence.test.ts` exits 0
  </acceptance_criteria>
  <action>Extend pipeline event contract with explicit schema version and audit detail fields for AI interactions/gate decisions (D-13). Emit enriched correlation details from participant and orchestrator event sources, then validate persisted logs preserve requestId/timestamp/stage/action continuity and decision metadata required for SECU-03 review.</action>
  <verify>
    <automated>npm run test -- tests/integration/audit-persistence-request-correlation.test.ts tests/integration/audit-redaction-persistence.test.ts</automated>
  </verify>
  <done>Audit logs provide complete, redacted, request-correlated trace records for all AI and gate decision events.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/integration/audit-persistence-request-correlation.test.ts tests/integration/audit-redaction-persistence.test.ts tests/integration/request-correlation.test.ts
</verification>

<success_criteria>
- SECU-03 satisfied: all AI interactions + gate decisions are persisted with request correlation.
- SECU-04 satisfied: redaction is applied pre-persistence and evidence metadata is stored per record.
- D-10, D-11, D-12, D-13 are encoded in dual-sink wiring, file persistence path, and integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-execution-retry-loop-and-audit-logging/05-03-SUMMARY.md`
</output>
