---
phase: 05-execution-retry-loop-and-audit-logging
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/pipeline/orchestrator.ts
  - src/pipeline/stateMachine.ts
  - src/participant/handler.ts
  - src/pipeline/execution/scopedRunExecutor.ts
  - tests/integration/execution-run-flow.test.ts
  - tests/integration/execution-retry-escalation.test.ts
autonomous: true
requirements: [RUN-01, RUN-03]
user_setup: []
must_haves:
  truths:
    - User can explicitly trigger scoped execution from workflow after write/preview completion.
    - Failed runs get exactly one scoped auto-fix retry; unresolved failures block progression pending user decision.
    - `continue` reruns same scope after manual fix confirmation, while `reject`/`cancel` terminate and `approve` acknowledges unresolved state.
  artifacts:
    - src/pipeline/orchestrator.ts adds run-trigger orchestration, scoped retry flow, and guardrail decision handling for execution failures.
    - src/pipeline/stateMachine.ts supports legal transitions needed for execution escalation from completed flow.
    - src/participant/handler.ts exposes run trigger and run-escalation decision wiring through existing quick-action semantics.
  key_links:
    - write outcomes -> execution scope capture -> `executeScopedRun` invocation path.
    - run failure -> one-shot scoped fix -> rerun -> escalation bundle -> blocked decision gate.
    - decision action (`approve|reject|continue|cancel`) -> orchestrator transition -> rerun/terminate/ack behavior.
---

<objective>
Integrate scoped run execution into orchestrator flow with one-shot repair and explicit escalation decision semantics.

Purpose: Fulfill RUN-01 workflow trigger behavior and RUN-03 controlled remediation boundary.
Output: Orchestrator + participant run trigger path, single-retry execution remediation loop, and decision-gated unresolved failure handling.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-RESEARCH.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-PATTERNS.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-01-PLAN.md
@.planning/phases/04-generation-preview-and-safe-file-writing/04-05-SUMMARY.md
@src/pipeline/guardrails/retryEscalation.ts
@src/participant/actions.ts

<interfaces>
From `src/pipeline/guardrails/retryEscalation.ts`:
```ts
export interface LintTypeEscalationBundle {
  command: string;
  topErrors: string[];
  affectedFiles: string[];
  attemptedFixSummary: string;
  suggestedActions: QuickAction[];
}
```

From `src/participant/actions.ts`:
```ts
export const QUICK_ACTIONS = ['approve', 'reject', 'continue', 'cancel'] as const;
export type QuickAction = (typeof QUICK_ACTIONS)[number];
```

From `src/pipeline/stateMachine.ts`:
```ts
export type PipelineState =
  | 'ready_to_write'
  | 'awaiting_guardrail_decision'
  | 'completed'
  | 'cancelled';
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run failure -> auto-fix retry path | Unscoped edits can mutate helpers/fixtures and violate governance |
| blocked escalation -> decision action | Invalid or bypassed actions can resume flow without explicit operator control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | `orchestrator.ts` execution retry path | mitigate | Enforce one retry max and hard-scope auto-fix targets to current generated/updated files only (D-07, D-08) |
| T-05-05 | E | `orchestrator.ts` decision handling | mitigate | Keep blocked state until allowlisted action captured; reject unmapped actions and log decision record with comment (D-09, D-15, D-16) |
| T-05-06 | D | `stateMachine.ts` transitions | mitigate | Add explicit legal transitions for execution escalation path and deny illegal resume paths that bypass run gate (ASVS L1 V4) |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire user-triggered scoped run entry into orchestrator and participant workflow</name>
  <files>src/pipeline/orchestrator.ts, src/pipeline/stateMachine.ts, src/participant/handler.ts, tests/integration/execution-run-flow.test.ts</files>
  <read_first>src/pipeline/orchestrator.ts, src/pipeline/stateMachine.ts, src/participant/handler.ts, tests/integration/execution-run-flow.test.ts, src/pipeline/execution/scopedRunExecutor.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md</read_first>
  <behavior>
    - User can invoke scoped run only from valid post-write state in current request flow.
    - Orchestrator emits command preview before launching execution command.
    - Full-suite path remains explicit opt-in only.
  </behavior>
  <acceptance_criteria>
    - `rg "executeScopedRun\\(|execution_run_requested|execution_command_preview" src/pipeline/orchestrator.ts src/participant/handler.ts` returns 1+ matches (D-02, RUN-01)
    - `rg "full_suite_opt_in|scopeMode" src/pipeline/orchestrator.ts src/pipeline/execution/scopedRunExecutor.ts` returns 1+ matches (D-03)
    - `rg "completed.*awaiting_guardrail_decision|awaiting_guardrail_decision.*completed" src/pipeline/stateMachine.ts` returns 1+ matches
    - `npm run test -- tests/integration/execution-run-flow.test.ts` exits 0
  </acceptance_criteria>
  <action>Add run entrypoint wiring: participant helper calls `orchestrator.executeScopedRun(requestId, options)` after write/preview completion (D-02). In orchestrator, guard allowed starting states, emit `execution_run_requested` + `execution_command_preview` events with exact command payload, and keep full-suite execution behind explicit `full_suite_opt_in` option only (D-03).</action>
  <verify>
    <automated>npm run test -- tests/integration/execution-run-flow.test.ts</automated>
  </verify>
  <done>Workflow exposes explicit scoped run trigger with command preview and no default full-suite widening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one-shot execution repair loop with scoped auto-fix boundary and escalation bundle</name>
  <files>src/pipeline/orchestrator.ts, src/pipeline/execution/scopedRunExecutor.ts, tests/integration/execution-retry-escalation.test.ts</files>
  <read_first>src/pipeline/orchestrator.ts, src/pipeline/execution/scopedRunExecutor.ts, tests/integration/execution-retry-escalation.test.ts, src/pipeline/guardrails/retryEscalation.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md</read_first>
  <behavior>
    - Exactly one run auto-fix retry is attempted after first failure.
    - Auto-fix target scope is constrained to generated/updated tests from current request only.
    - Retry failure emits structured escalation bundle and blocks progression.
  </behavior>
  <acceptance_criteria>
    - `rg "maxAttempts\\s*[:=]\\s*1|single retry|one-shot" src/pipeline/orchestrator.ts src/pipeline/execution/scopedRunExecutor.ts` returns 1+ matches (D-07)
    - `rg "generated\\|updated|targetFiles|scope" src/pipeline/orchestrator.ts` returns 1+ matches (D-08)
    - `rg "command|topErrors|affectedFiles|attemptedFixSummary|suggestedActions" src/pipeline/orchestrator.ts` returns 1+ matches (D-09)
    - `npm run test -- tests/integration/execution-retry-escalation.test.ts` exits 0
  </acceptance_criteria>
  <action>Reuse the escalation bundle pattern from phase-4 guardrails for run failures: attempt exactly one scoped auto-fix (D-07), reject any broadened edit scope beyond current generated/updated targets (D-08), rerun once, and on failure transition to blocked gate with complete escalation payload (D-09).</action>
  <verify>
    <automated>npm run test -- tests/integration/execution-retry-escalation.test.ts</automated>
  </verify>
  <done>Run remediation is bounded, scope-safe, and produces deterministic escalation details when unresolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce post-retry decision semantics (`continue`, `approve`, `reject`, `cancel`) for execution failures</name>
  <files>src/pipeline/orchestrator.ts, src/pipeline/stateMachine.ts, src/participant/handler.ts, tests/integration/execution-retry-escalation.test.ts</files>
  <read_first>src/pipeline/orchestrator.ts, src/pipeline/stateMachine.ts, src/participant/handler.ts, tests/integration/execution-retry-escalation.test.ts, src/participant/actions.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md</read_first>
  <behavior>
    - `continue` records manual-fix acknowledgment and reruns same scoped test command.
    - `approve` records explicit acceptance and closes blocked gate without rerun.
    - `reject`/`cancel` terminate current execution flow.
  </behavior>
  <acceptance_criteria>
    - `rg "guardrail_decision_recorded|manual_fix_confirmed|execution_rerun_requested" src/pipeline/orchestrator.ts` returns 1+ matches (D-14)
    - `rg "approve|reject|continue|cancel" src/pipeline/orchestrator.ts src/participant/handler.ts` returns 1+ matches (D-15, D-16)
    - `rg "execution_decision_approved|execution_decision_cancelled|execution_decision_rejected" src/pipeline/orchestrator.ts` returns 1+ matches
    - `npm run test -- tests/integration/execution-retry-escalation.test.ts` exits 0
  </acceptance_criteria>
  <action>Implement explicit decision branching for unresolved run failures: `continue` means user-confirmed manual fix then rerun same scope (D-14), `approve` acknowledges current failed state and closes gate (D-16), and `reject`/`cancel` terminate execution (D-15). Persist decision comments in history for audit correlation.</action>
  <verify>
    <automated>npm run test -- tests/integration/execution-retry-escalation.test.ts</automated>
  </verify>
  <done>Blocked run failures cannot proceed silently; all outcomes require and persist explicit user decision semantics.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/integration/execution-run-flow.test.ts tests/integration/execution-retry-escalation.test.ts
</verification>

<success_criteria>
- RUN-01 satisfied: user-triggered scoped execution integrated into workflow with command preview.
- RUN-03 satisfied: one-shot repair loop + blocked escalation gate with explicit action semantics.
- D-07, D-08, D-09, D-14, D-15, D-16 are enforced by orchestrator/state-machine contracts and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-execution-retry-loop-and-audit-logging/05-02-SUMMARY.md`
</output>
