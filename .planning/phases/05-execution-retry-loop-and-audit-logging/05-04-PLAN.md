---
phase: 05-execution-retry-loop-and-audit-logging
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - src/pipeline/orchestrator.ts
  - src/participant/handler.ts
  - tests/integration/execution-run-flow.test.ts
  - tests/integration/execution-retry-escalation.test.ts
  - tests/integration/audit-persistence-request-correlation.test.ts
autonomous: true
gap_closure: true
requirements: [RUN-01, RUN-02, RUN-03, SECU-03, SECU-04]
user_setup: []
must_haves:
  truths:
    - Runtime execution responses include `buildExecutionRunSummary(...)` output with pass/fail totals, failing files, top errors, and bucket counts.
    - Runtime execution flow surfaces classifier-backed `bucket` and `bucketReason` diagnostics in run response payloads and escalation payloads.
    - Persisted audit details for execution events include bucket diagnostics while preserving existing redaction pipeline behavior.
  artifacts:
    - src/pipeline/orchestrator.ts wires summary/classification runtime path and includes diagnostics in emitted execution events.
    - src/participant/handler.ts preserves enriched execution response contract for participant consumers.
    - tests/integration/execution-run-flow.test.ts and tests/integration/execution-retry-escalation.test.ts assert runtime summary + diagnostics wiring.
    - tests/integration/audit-persistence-request-correlation.test.ts asserts persisted execution audit details carry bucket diagnostics.
  key_links:
    - Scoped run result -> failure extraction -> `buildExecutionRunSummary(...)` -> `ExecutionRunResult` payload.
    - `buildExecutionRunSummary(...)` internal `classifyExecutionFailures(...)` output -> execution event details (`bucket`, `bucketReason`) -> audit file sink persistence.
    - participant execution handlers -> orchestrator response passthrough -> chat/webview consumers receive enriched diagnostics.
---

<objective>
Close Phase 05 verification gaps by wiring execution summary/classification modules into runtime orchestrator and participant outputs.

Purpose: Resolve failed truths in `05-VERIFICATION.md` without rewriting completed plan intent.
Output: Gap-closure runtime wiring for execution summary DTO and classifier diagnostics across response payloads and audit records.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-RESEARCH.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-PATTERNS.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-VERIFICATION.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-REVIEW.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-01-SUMMARY.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-02-SUMMARY.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-03-SUMMARY.md
@src/pipeline/orchestrator.ts
@src/participant/handler.ts
@src/pipeline/execution/reportSummarizer.ts
@src/pipeline/execution/failureClassifier.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire execution summary DTO into runtime orchestrator and participant responses</name>
  <files>src/pipeline/orchestrator.ts, src/participant/handler.ts, tests/integration/execution-run-flow.test.ts</files>
  <read_first>src/pipeline/orchestrator.ts, src/participant/handler.ts, src/pipeline/execution/reportSummarizer.ts, src/pipeline/execution/scopedRunExecutor.ts, tests/integration/execution-run-flow.test.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-VERIFICATION.md</read_first>
  <behavior>
    - Runtime execution flow calls `buildExecutionRunSummary(...)` with real run command output and failure data.
    - `ExecutionRunResult` returned by orchestrator/participant carries summary DTO fields required by D-05.
    - Success and escalation paths both preserve summary payloads for downstream chat/webview rendering.
  </behavior>
  <acceptance_criteria>
    - `rg "buildExecutionRunSummary\\(" src/pipeline/orchestrator.ts` returns 1+ matches (gap-1 closure)
    - `rg "executionSummary|runSummary|summary" src/pipeline/orchestrator.ts src/participant/handler.ts` returns 1+ matches (D-05, RUN-02)
    - `rg "passCount|failCount|failingFiles|topErrors|bucketCounts" tests/integration/execution-run-flow.test.ts` returns 1+ matches
    - `npm run test -- tests/integration/execution-run-flow.test.ts` exits 0
  </acceptance_criteria>
  <action>Update execution runtime wiring in `executeScopedRun(...)` so run results are transformed through `buildExecutionRunSummary(...)` (implements D-05 per failed truth #1). Extend `ExecutionRunResult` contract to carry summary DTO on both successful runs and guardrail escalation paths; keep `handleExecutionRunRequest(...)`/`handleExecutionGuardrailDecision(...)` passthrough behavior intact so participant consumers receive same enriched object. Do not add fallback placeholder values; derive fields from actual run output and parsed failures.</action>
  <verify>
    <automated>npm run test -- tests/integration/execution-run-flow.test.ts</automated>
  </verify>
  <done>Verifier gap 1 is closed: runtime path now emits summary DTO instead of leaving summarizer orphaned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface classifier bucket diagnostics in execution responses and persisted audit details</name>
  <files>src/pipeline/orchestrator.ts, tests/integration/execution-retry-escalation.test.ts, tests/integration/audit-persistence-request-correlation.test.ts</files>
  <read_first>src/pipeline/orchestrator.ts, src/pipeline/execution/failureClassifier.ts, src/pipeline/execution/reportSummarizer.ts, tests/integration/execution-retry-escalation.test.ts, tests/integration/audit-persistence-request-correlation.test.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-VERIFICATION.md</read_first>
  <behavior>
    - Runtime path surfaces classifier-derived `bucket` and `bucketReason` diagnostics for failed targets.
    - Execution escalation and completion events include bucket diagnostics in emitted details so audit sink persists them.
    - Diagnostic payload remains redaction-safe by reusing existing sanitized run outputs and sink redaction path.
  </behavior>
  <acceptance_criteria>
    - `rg "bucketReason|bucket" src/pipeline/orchestrator.ts` returns 1+ runtime matches tied to execution response/event details (gap-2 closure)
    - `rg "execution_run_(succeeded|escalated)|bucketReason|bucket" tests/integration/execution-retry-escalation.test.ts` returns 1+ matches
    - `rg "bucketReason|bucket|execution_run_" tests/integration/audit-persistence-request-correlation.test.ts` returns 1+ matches (SECU-03 link)
    - `npm run test -- tests/integration/execution-retry-escalation.test.ts tests/integration/audit-persistence-request-correlation.test.ts` exits 0
  </acceptance_criteria>
  <action>Use runtime summary/classification output (D-04, D-06) to attach deterministic bucket diagnostics to execution response payloads and execution event details that are persisted by dual sinks (SECU-03). Ensure escalation bundle context includes classifier evidence per failing target where available, and keep all emitted diagnostics flowing through existing redaction-before-persist path (D-12, SECU-04). This task directly closes failed truth #2 from `05-VERIFICATION.md`.</action>
  <verify>
    <automated>npm run test -- tests/integration/execution-retry-escalation.test.ts tests/integration/audit-persistence-request-correlation.test.ts</automated>
  </verify>
  <done>Verifier gap 2 is closed: runtime classifier diagnostics appear in execution outputs and persisted audit details.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run output parsing -> execution response payload | Miswired or spoofed diagnostics can hide true root cause and mislead gate decisions |
| execution event details -> persistent audit sink | Missing/incorrect bucket diagnostics weaken forensic traceability and decision accountability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | T | `src/pipeline/orchestrator.ts` runtime summary wiring | mitigate | Build summary only from actual scoped run result and parsed failures; no hardcoded/placeholder diagnostic fields (D-05, D-06) |
| T-05-11 | R | execution response + audit diagnostics | mitigate | Include deterministic `bucket` + `bucketReason` in execution payloads/events for post-run audit replay (D-04, D-13) |
| T-05-12 | I | execution diagnostics persisted via sink | mitigate | Keep diagnostics on existing sanitized/redacted data path and verify no regression in persistence tests (D-12, SECU-04) |
</threat_model>

<verification>
- [ ] npm run test -- tests/integration/execution-run-flow.test.ts
- [ ] npm run test -- tests/integration/execution-retry-escalation.test.ts tests/integration/audit-persistence-request-correlation.test.ts
- [ ] npm run test -- tests/integration/execution-classification-reporting.test.ts
</verification>

<success_criteria>
- Gap truth 1 from `05-VERIFICATION.md` passes: `buildExecutionRunSummary(...)` is invoked in runtime execution flow and returned in orchestrator/participant outputs.
- Gap truth 2 from `05-VERIFICATION.md` passes: classifier diagnostics (`bucket`, `bucketReason`) are surfaced in execution response payloads and persisted audit details.
- Existing completed plans (`05-01`..`05-03`) remain unchanged; gap closure is additive only via `05-04-PLAN.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-execution-retry-loop-and-audit-logging/05-04-SUMMARY.md`
</output>
