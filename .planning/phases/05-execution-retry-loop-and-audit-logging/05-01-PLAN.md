---
phase: 05-execution-retry-loop-and-audit-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/execution/contracts.ts
  - src/pipeline/execution/scopedRunExecutor.ts
  - src/pipeline/execution/failureClassifier.ts
  - src/pipeline/execution/reportSummarizer.ts
  - tests/integration/execution-run-flow.test.ts
  - tests/integration/execution-classification-reporting.test.ts
autonomous: true
requirements: [RUN-01, RUN-02]
user_setup: []
must_haves:
  truths:
    - User-triggered execution runs only generated/updated test targets from current request by default.
    - Run summary shows pass/fail totals, failing files, top errors, and root-cause buckets.
    - Run report clearly distinguishes likely test-authoring vs application/environment failures.
  artifacts:
    - src/pipeline/execution/scopedRunExecutor.ts builds command preview and executes scoped Playwright run contracts.
    - src/pipeline/execution/failureClassifier.ts maps failures to `test_authoring`, `application_behavior`, `environment_or_tooling`.
    - src/pipeline/execution/reportSummarizer.ts emits concise-first summary with expandable raw stderr/stdout payload.
  key_links:
    - generated/updated file set -> scoped command assembly -> local tool command runner.
    - Playwright run result -> classifier buckets -> summary report DTO.
    - report DTO -> participant/orchestrator response payload for chat + webview rendering.
---

<objective>
Define execution-domain contracts for scoped Playwright runs and deterministic failure reporting.

Purpose: Deliver RUN-01/RUN-02 building blocks that satisfy scoped execution defaults and required diagnostic clarity.
Output: Execution contracts, scoped command runner, failure classifier, and summary formatter with integration test coverage.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-RESEARCH.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-PATTERNS.md
@.planning/phases/05-execution-retry-loop-and-audit-logging/05-VALIDATION.md
@.planning/phases/04-generation-preview-and-safe-file-writing/04-05-SUMMARY.md
@src/adapters/localToolRunner.ts
@src/pipeline/writer/writeReport.ts

<interfaces>
From `src/adapters/localToolRunner.ts`:
```ts
export interface LocalToolCommandResult {
  ok: boolean;
  command: string;
  args: string[];
  exitCode: number | null;
  stdout: string;
  stderr: string;
  timedOut: boolean;
  error?: string;
}
```

From `src/pipeline/writer/writeReport.ts`:
```ts
export interface WriteReport {
  requestId: string;
  previewVersion: string;
  summary: WriteReportSummary;
  outcomes: WriteOutcome[];
  skippedReasons: Partial<Record<AnchorUnsafeReason, number>>;
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestration scope -> command execution | Scope widening can run unrelated tests and leak noisy failures into run decisions |
| command outputs -> user-facing report | Raw output can misclassify failures or expose unredacted sensitive text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `scopedRunExecutor.ts` | mitigate | Enforce generated/updated target normalization with deterministic sort and explicit full-suite opt-in branch only (D-01, D-03) |
| T-05-02 | I | `reportSummarizer.ts` | mitigate | Keep concise summary fields explicit and attach raw output in separate expandable payload while preserving existing redaction path (D-05) |
| T-05-03 | R | `failureClassifier.ts` | mitigate | Emit deterministic bucket rationale keyed to classifier evidence so RUN-02 audit review can trace why failure was labeled test/app/env (D-04, D-06) |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create scoped run contracts and executor with explicit command preview</name>
  <files>src/pipeline/execution/contracts.ts, src/pipeline/execution/scopedRunExecutor.ts, tests/integration/execution-run-flow.test.ts</files>
  <read_first>src/pipeline/execution/contracts.ts, src/pipeline/execution/scopedRunExecutor.ts, tests/integration/execution-run-flow.test.ts, src/adapters/localToolRunner.ts, src/pipeline/writer/writeContracts.ts, src/pipeline/guardrails/lintTypeRunner.ts, src/pipeline/writer/writeReport.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md, .planning/phases/05-execution-retry-loop-and-audit-logging/05-PATTERNS.md</read_first>
  <behavior>
    - Default run scope is generated/updated test targets only.
    - Command preview is produced before execution and includes exact command/args.
    - Full-suite path exists only as explicit opt-in mode.
  </behavior>
  <acceptance_criteria>
    - `rg "generated\\|updated|scopeMode|full_suite_opt_in" src/pipeline/execution/contracts.ts src/pipeline/execution/scopedRunExecutor.ts` returns 1+ matches (D-01, D-03)
    - `rg "commandPreview|playwright|--reporter=json" src/pipeline/execution/scopedRunExecutor.ts` returns 1+ matches (D-02)
    - `rg "execution_command_preview|execution_run_started" tests/integration/execution-run-flow.test.ts` returns 1+ matches
    - `npm run test -- tests/integration/execution-run-flow.test.ts` exits 0
  </acceptance_criteria>
  <action>Implement execution contracts and `executeScopedRun(...)` service that derives default targets from current request generated/updated files (D-01), emits an explicit command preview payload before shell launch (D-02), and supports full-suite only through an explicit `full_suite_opt_in` branch (D-03). Follow PATTERNS analogs explicitly: `src/pipeline/writer/writeContracts.ts` for contract normalization/literal-union shape, and `src/pipeline/guardrails/lintTypeRunner.ts` for command-runner dependency injection + sanitized command-result handling in scoped executor. Reuse `runLocalToolCommand` for process execution and existing redaction behavior.</action>
  <verify>
    <automated>npm run test -- tests/integration/execution-run-flow.test.ts</automated>
  </verify>
  <done>Scoped execution contract exists, command preview is deterministic, and default path does not widen scope beyond generated/updated targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic failure buckets and concise-first run summary formatter</name>
  <files>src/pipeline/execution/failureClassifier.ts, src/pipeline/execution/reportSummarizer.ts, tests/integration/execution-classification-reporting.test.ts</files>
  <read_first>src/pipeline/execution/failureClassifier.ts, src/pipeline/execution/reportSummarizer.ts, tests/integration/execution-classification-reporting.test.ts, .planning/phases/05-execution-retry-loop-and-audit-logging/05-CONTEXT.md, .planning/phases/05-execution-retry-loop-and-audit-logging/05-RESEARCH.md, src/pipeline/context/confluenceRelevance.ts, src/pipeline/writer/writeReport.ts</read_first>
  <behavior>
    - Classifier emits only required buckets: `test_authoring`, `application_behavior`, `environment_or_tooling`.
    - Summary payload leads with pass/fail counts, failing files, and top errors.
    - Raw stderr/stdout remains available as expandable details separate from concise summary.
  </behavior>
  <acceptance_criteria>
    - `rg "test_authoring|application_behavior|environment_or_tooling" src/pipeline/execution/failureClassifier.ts` returns 1+ matches (D-04)
    - `rg "passCount|failCount|failingFiles|topErrors" src/pipeline/execution/reportSummarizer.ts` returns 1+ matches (D-05)
    - `rg "rawStdout|rawStderr|expandable|bucketReason" src/pipeline/execution/reportSummarizer.ts` returns 1+ matches (D-05, D-06)
    - `npm run test -- tests/integration/execution-classification-reporting.test.ts` exits 0
  </acceptance_criteria>
  <action>Implement deterministic classifier and report formatter: classify each failing result into D-04 buckets, build concise-first summary fields per D-05, and include raw output payloads plus explicit bucket rationale to satisfy RUN-02/D-06 test-vs-app distinction requirements.</action>
  <verify>
    <automated>npm run test -- tests/integration/execution-classification-reporting.test.ts</automated>
  </verify>
  <done>Run reporting produces required buckets and concise-first diagnostics with raw detail preserved for expansion.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/integration/execution-run-flow.test.ts tests/integration/execution-classification-reporting.test.ts
</verification>

<success_criteria>
- RUN-01 foundation exists: scoped execution service + explicit command preview + full-suite opt-in branch.
- RUN-02 satisfied: deterministic root-cause bucketing and concise-first summary with raw detail expansion path.
- D-01..D-06 are encoded in contracts/tests without default scope widening.
</success_criteria>

<output>
After completion, create `.planning/phases/05-execution-retry-loop-and-audit-logging/05-01-SUMMARY.md`
</output>
