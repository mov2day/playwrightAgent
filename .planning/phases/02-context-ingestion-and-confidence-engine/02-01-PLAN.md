---
phase: 02-context-ingestion-and-confidence-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/repoAnalysis/contracts.ts
  - src/pipeline/repoAnalysis/detectors/frameworkDetector.ts
  - src/pipeline/repoAnalysis/detectors/patternDetector.ts
  - src/pipeline/repoAnalysis/detectors/reuseDetector.ts
  - src/pipeline/repoAnalysis/repoAnalyzer.ts
  - src/pipeline/repoAnalysis/summary.ts
  - tests/unit/repo-analyzer.test.ts
autonomous: true
requirements: [REPO-01, REPO-02, REPO-03, REPO-04]
user_setup: []
must_haves:
  truths:
    - Repo analyzer runs before planning and emits deterministic typed findings.
    - Pattern detection supports multi-label output with per-pattern confidence.
    - Reuse candidates are detected via deterministic heuristics first, AI tie-break second.
  artifacts:
    - src/pipeline/repoAnalysis/contracts.ts defines finding/result contracts and summary payload.
    - src/pipeline/repoAnalysis/repoAnalyzer.ts exports orchestration entrypoint returning summary + findings.
    - tests/unit/repo-analyzer.test.ts covers unknown fallback and confidence penalty behavior.
  key_links:
    - Participant/pipeline request flow can call repoAnalyzer before context fusion.
    - Confidence engine later consumes repo summary fields from repoAnalyzer output.
---

<objective>
Implement the repository analyzer service and pattern classifier contracts.

Purpose: Produce trustworthy repo signals (framework, architecture style, reusable assets) before Jira/Confluence scoring stages.
Output: Typed analyzer contracts, detector implementations, analyzer orchestrator, and unit test coverage.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-RESEARCH.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-PATTERNS.md
@src/pipeline/contracts.ts
@src/participant/handler.ts
@skills/playwright-skill/run.js
</context>

<threat_model>
<trust_boundaries>
- Repository file-system boundary: analyzer reads local repo files and metadata.
- Classification boundary: detector outputs become confidence inputs and must be tamper-evident.
- Explainability boundary: analyzer evidence can be surfaced to user and must be sanitized.
</trust_boundaries>

<threat_register>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | detector output | mitigate | typed result contracts + deterministic detector ordering |
| T-02-02 | Repudiation | evidence provenance | mitigate | include evidence refs/snippets and detector IDs for each finding |
| T-02-03 | Denial of service | repo scan breadth | mitigate | bounded scan scope, file-count limits, and early exits |
</threat_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Define repo-analyzer contract types and scoring metadata</name>
  <files>src/pipeline/repoAnalysis/contracts.ts, src/pipeline/repoAnalysis/summary.ts</files>
  <read_first>.planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md, src/pipeline/contracts.ts, src/pipeline/events.ts</read_first>
  <action>Create `AnalyzerFinding`, `PatternClassification`, `ReuseCandidate`, and `RepoAnalysisSummary` interfaces with exact fields from context decisions: `id`, `category`, `result`, `confidence`, `evidence[]`, `notes`, `primaryPattern`, `secondaryPatterns[]`, `unknownReason`, and `confidencePenalty`. Add `RepoAnalysisSummary` fields `framework`, `pattern`, `reuseCandidates`, `overallConfidence`, and `warnings`.</action>
  <verify>npm run test -- tests/unit/repo-analyzer.test.ts</verify>
  <acceptance_criteria>
    - `src/pipeline/repoAnalysis/contracts.ts` contains interface `AnalyzerFinding` with field `evidence`
    - `src/pipeline/repoAnalysis/contracts.ts` contains interface `PatternClassification` with fields `primaryPattern` and `secondaryPatterns`
    - `src/pipeline/repoAnalysis/summary.ts` exports a function named `buildRepoAnalysisSummary`
  </acceptance_criteria>
  <done>Analyzer output contract is explicit and ready for detector implementations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic-first detectors with multi-label pattern output</name>
  <files>src/pipeline/repoAnalysis/detectors/frameworkDetector.ts, src/pipeline/repoAnalysis/detectors/patternDetector.ts, src/pipeline/repoAnalysis/detectors/reuseDetector.ts</files>
  <read_first>.planning/phases/02-context-ingestion-and-confidence-engine/02-RESEARCH.md, .planning/codebase/STRUCTURE.md, src/participant/slashPlanParser.ts</read_first>
  <action>Implement framework detector (Node/TypeScript/Vitest signals), pattern detector (POM/Screenplay/hybrid heuristics), and reuse detector (exports/import graph + naming/path heuristics). In `patternDetector.ts`, return `primaryPattern`, `secondaryPatterns[]`, and per-pattern confidence map. Add tie-break hook `resolvePatternTieBreakWithSemanticHint(input)` returning optional override; if absent, keep deterministic ranking.</action>
  <verify>npm run test -- tests/unit/repo-analyzer.test.ts</verify>
  <acceptance_criteria>
    - `patternDetector.ts` contains exported function `detectPatternClassification`
    - `reuseDetector.ts` contains exported function `detectReuseCandidates`
    - `frameworkDetector.ts` emits at least one finding category `framework`
  </acceptance_criteria>
  <done>Detectors produce deterministic findings and preserve extensibility for semantic tie-breaks.</done>
</task>

<task type="auto">
  <name>Task 3: Build analyzer orchestrator, unknown fallback, and report summary output</name>
  <files>src/pipeline/repoAnalysis/repoAnalyzer.ts, tests/unit/repo-analyzer.test.ts</files>
  <read_first>src/pipeline/repoAnalysis/contracts.ts, src/pipeline/repoAnalysis/detectors/patternDetector.ts, .planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md</read_first>
  <action>Implement `analyzeRepositoryContext(input)` that executes detectors in fixed order, aggregates findings, sets `unknown` markers when confidence `< 0.5` for critical detectors, and applies explicit `confidencePenalty` field in summary. Add tests for: (1) hybrid pattern result, (2) low-confidence unknown fallback, and (3) summary includes reusable assets list and warnings.</action>
  <verify>npm run test -- tests/unit/repo-analyzer.test.ts</verify>
  <acceptance_criteria>
    - `repoAnalyzer.ts` exports `analyzeRepositoryContext`
    - low-confidence path sets `result: unknown` in at least one finding
    - test file contains test case name including `unknown fallback`
  </acceptance_criteria>
  <done>Repo analyzer is executable and satisfies REPO-01..REPO-04 behavior contracts.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/repo-analyzer.test.ts
</verification>

<success_criteria>
- Analyzer runs deterministically and returns typed evidence-bearing findings.
- Pattern classification supports multi-label output with confidence.
- Summary payload is suitable for confidence engine input and user-facing reporting.
</success_criteria>

<output>
After completion, create .planning/phases/02-context-ingestion-and-confidence-engine/02-01-SUMMARY.md.
</output>
