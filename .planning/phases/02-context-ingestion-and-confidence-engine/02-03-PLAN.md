---
phase: 02-context-ingestion-and-confidence-engine
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/adapters/confluenceClient.ts
  - src/pipeline/context/confluenceQueryBuilder.ts
  - src/pipeline/context/confluenceRelevance.ts
  - src/pipeline/context/confluenceContextBuilder.ts
  - tests/unit/confluence-query-builder.test.ts
  - tests/unit/confluence-relevance.test.ts
autonomous: true
requirements: [CONF-01, CONF-02]
user_setup: []
must_haves:
  truths:
    - Confluence retrieval queries are derived from Jira context and run via local tooling.
    - Relevance scoring is hybrid (lexical, semantic, link proximity) with configurable weights.
    - Low relevance is excluded, mid relevance is neutral-visible, high relevance contributes bonus.
  artifacts:
    - src/pipeline/context/confluenceQueryBuilder.ts emits query payload from Jira entities.
    - src/pipeline/context/confluenceRelevance.ts implements thresholds and freshness decay.
    - src/pipeline/context/confluenceContextBuilder.ts returns scoring-ready normalized context.
  key_links:
    - confluenceContextBuilder consumes jiraContextBuilder output from Plan 02-02.
    - confidence engine in Plan 02-04 consumes high/mid/low buckets and relevance reasons.
---

<objective>
Implement Confluence local-tool adapter, query generation, and relevance scoring pipeline.

Purpose: Add high-quality Confluence signal enrichment without allowing low-value pages to pollute confidence or planning context.
Output: Confluence adapter, query builder, relevance engine, context builder, and tests.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-RESEARCH.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-02-PLAN.md
@src/adapters/jiraClient.ts
@src/pipeline/context/jiraContextBuilder.ts
@src/adapters/eventSink.ts
</context>

<threat_model>
<trust_boundaries>
- Query synthesis boundary: Jira-derived entity data is transformed into Confluence lookup queries.
- Relevance boundary: ranking decisions directly affect confidence score bonuses.
- Content exposure boundary: Confluence snippets can include sensitive/irrelevant data.
</trust_boundaries>

<threat_register>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | relevance scoring | mitigate | deterministic weighted scoring with explainability fields |
| T-02-08 | Information disclosure | page snippets | mitigate | sanitize snippets and keep only bounded excerpts |
| T-02-09 | Integrity | Jira/Confluence precedence | mitigate | enforce augmentation-only rule (never override Jira authority) |
</threat_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Build Confluence query builder from Jira context graph</name>
  <files>src/adapters/confluenceClient.ts, src/pipeline/context/confluenceQueryBuilder.ts, tests/unit/confluence-query-builder.test.ts</files>
  <read_first>src/pipeline/context/jiraContextBuilder.ts, .planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md, docs/tool.md</read_first>
  <action>Create `ConfluenceClient` adapter contract and query-builder function `buildConfluenceQueriesFromJiraContext(context)` that extracts issue keys, epic titles, acceptance keywords, component names, and linked page IDs from Jira context. Emit query objects with fields `queryText`, `sourceEntity`, `priority`, and `maxResults`.
</action>
  <verify>npm run test -- tests/unit/confluence-query-builder.test.ts</verify>
  <acceptance_criteria>
    - `confluenceQueryBuilder.ts` exports `buildConfluenceQueriesFromJiraContext`
    - query object includes fields `queryText`, `priority`, and `sourceEntity`
    - test verifies at least one query includes Jira issue key token
  </acceptance_criteria>
  <done>Confluence lookup queries are deterministic and grounded in Jira context.</done>
</task>

<task type="auto">
  <name>Task 2: Implement hybrid relevance scoring with threshold buckets and freshness decay</name>
  <files>src/pipeline/context/confluenceRelevance.ts, tests/unit/confluence-relevance.test.ts</files>
  <read_first>.planning/phases/02-context-ingestion-and-confidence-engine/02-RESEARCH.md, .planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md, src/pipeline/repoAnalysis/contracts.ts</read_first>
  <action>Implement scorer `scoreConfluencePage(page, jiraContext, weights, now)` using weighted components: `lexical`, `semantic`, `jiraLinkProximity`, and `freshness`. Return score plus bucket classification: `high` (bonus), `mid` (neutral-visible), `low` (excluded). Add freshness decay based on page age days; enforce non-override flag `augmentationOnly: true` in result metadata.</action>
  <verify>npm run test -- tests/unit/confluence-relevance.test.ts</verify>
  <acceptance_criteria>
    - scorer output includes fields `score`, `bucket`, `componentScores`, and `augmentationOnly`
    - tests include threshold cases named `high bonus`, `mid neutral`, and `low excluded`
    - stale content test verifies score decreases as `lastUpdatedAt` age increases
  </acceptance_criteria>
  <done>Relevance scoring behavior satisfies CONF-01 and CONF-02 with explainable thresholds.</done>
</task>

<task type="auto">
  <name>Task 3: Build normalized Confluence context output for confidence engine consumption</name>
  <files>src/pipeline/context/confluenceContextBuilder.ts, tests/unit/confluence-relevance.test.ts</files>
  <read_first>src/pipeline/context/confluenceQueryBuilder.ts, src/pipeline/context/confluenceRelevance.ts, src/adapters/confluenceClient.ts</read_first>
  <action>Implement `buildConfluenceContext(jiraContext, options)` that executes queries via adapter, scores each page, excludes `low` bucket pages from scoring payload, keeps `mid` pages in `visibleOnly` collection, and keeps `high` pages in `bonusCandidates`. Include aggregated summary fields `highCount`, `midCount`, `excludedLowCount`, and `bonusContributionPotential`.</action>
  <verify>npm run test -- tests/unit/confluence-relevance.test.ts</verify>
  <acceptance_criteria>
    - output contains arrays `bonusCandidates` and `visibleOnly`
    - output contains numeric fields `highCount`, `midCount`, `excludedLowCount`
    - low bucket pages are absent from scoring contribution list
  </acceptance_criteria>
  <done>Confluence context output is quality-gated and ready for confidence composition.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/confluence-query-builder.test.ts
- [ ] npm run test -- tests/unit/confluence-relevance.test.ts
</verification>

<success_criteria>
- Confluence queries are Jira-context-driven and tooling-local.
- Relevance scoring enforces high/mid/low policy with freshness decay.
- Output structure cleanly separates bonus candidates from neutral-visible context.
</success_criteria>

<output>
After completion, create .planning/phases/02-context-ingestion-and-confidence-engine/02-03-SUMMARY.md.
</output>
