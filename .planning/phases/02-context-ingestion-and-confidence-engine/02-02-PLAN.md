---
phase: 02-context-ingestion-and-confidence-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/localToolRunner.ts
  - src/adapters/jiraClient.ts
  - src/adapters/jiraGraphTraversal.ts
  - src/adapters/jiraAttachmentPolicy.ts
  - src/pipeline/context/jiraContextBuilder.ts
  - tests/unit/jira-client.test.ts
  - tests/unit/jira-graph-traversal.test.ts
autonomous: true
requirements: [JIRA-01, JIRA-02, JIRA-03, JIRA-04, JIRA-05, JIRA-06]
user_setup: []
must_haves:
  truths:
    - Jira fetch must be local-tool-only and never expose credentials to model context.
    - Task/subtask/epic/linked traversal rules are mandatory and cycle-safe.
    - Traversal must enforce caps/time budgets and expose completeness/truncation metadata.
  artifacts:
    - src/adapters/jiraClient.ts provides local-tool adapter interface and implementation.
    - src/adapters/jiraGraphTraversal.ts implements global visited-set dedupe with provenance edges.
    - src/pipeline/context/jiraContextBuilder.ts returns normalized Jira context payload for scoring.
  key_links:
    - jiraContextBuilder output is consumed by Confluence query builder and confidence engine.
    - localToolRunner enforces safe process invocation and redaction-aware error surfaces.
---

<objective>
Implement Jira local-tool adapter and deep traversal contracts.

Purpose: Build secure, bounded, and explainable Jira graph ingestion that satisfies all required deep-fetch semantics.
Output: Tool runner, Jira client, traversal/attachment policies, context builder, and unit tests.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-RESEARCH.md
@src/adapters/eventSink.ts
@src/pipeline/orchestrator.ts
@docs/tool.md
</context>

<threat_model>
<trust_boundaries>
- Secret boundary: Jira credentials remain process-local and must not enter AI prompt/event payloads.
- Tool invocation boundary: local adapter executes subprocesses and receives untrusted textual output.
- Graph expansion boundary: linked traversal can grow rapidly and must be bounded.
</trust_boundaries>

<threat_register>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information disclosure | local tool invocation | mitigate | redact env/token values in errors and logs; pass only request-scoped IDs to events |
| T-02-05 | Denial of service | traversal recursion | mitigate | global visited set + hard caps (`maxIssues`, `maxEdges`) + stage timeout |
| T-02-06 | Tampering | linked graph payload | mitigate | normalize entities by key, validate schema, preserve provenance edges |
</threat_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Create local tooling runner and Jira client contract</name>
  <files>src/adapters/localToolRunner.ts, src/adapters/jiraClient.ts, tests/unit/jira-client.test.ts</files>
  <read_first>.planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md, src/adapters/eventSink.ts, src/pipeline/contracts.ts</read_first>
  <action>Create `runLocalToolCommand(command, args, timeoutMs)` wrapper using `child_process.spawn` with `AbortController` timeout and bounded stdout/stderr capture. In `jiraClient.ts`, define `JiraClient` contract returning typed payload (`ticket`, `comments`, `attachments`, `linkedIssues`, `linkedPages`, `epic`, `parent`, `subtasks`, `completeness`). Ensure adapter never serializes env vars or credential strings into returned errors.</action>
  <verify>npm run test -- tests/unit/jira-client.test.ts</verify>
  <acceptance_criteria>
    - `localToolRunner.ts` exports function `runLocalToolCommand`
    - `jiraClient.ts` exports interface `JiraClient`
    - test case asserts credential-like tokens are redacted from error output
  </acceptance_criteria>
  <done>Local Jira tooling boundary is secure, typed, and testable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deep traversal policy with global dedupe and mandatory ticket-type rules</name>
  <files>src/adapters/jiraGraphTraversal.ts, src/pipeline/context/jiraContextBuilder.ts, tests/unit/jira-graph-traversal.test.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md, src/pipeline/stateMachine.ts</read_first>
  <action>Implement traversal engine with global visited sets (`issues`, `pages`), provenance edges (`from`, `to`, `relation`), and hard-cap counters (`maxIssues`, `maxPages`, `maxEdges`). Encode mandatory rules: task fetch includes full subtasks and linked graph; subtask fetch includes full parent and linked graph; epic relation always fetched when present; linked issues/pages recursively traversed under caps. Return `truncated` flags when any cap is hit.</action>
  <verify>npm run test -- tests/unit/jira-graph-traversal.test.ts</verify>
  <acceptance_criteria>
    - `jiraGraphTraversal.ts` contains a `visitedIssues` set and `visitedPages` set
    - traversal result includes `truncated` object with keys `issues`, `pages`, `edges`
    - tests include cases named `task deep fetch`, `subtask parent fetch`, and `always fetch epic`
  </acceptance_criteria>
  <done>Traversal logic satisfies JIRA-02..JIRA-06 with bounded deterministic behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add attachment policy, stage budgets, and completeness semantics</name>
  <files>src/adapters/jiraAttachmentPolicy.ts, src/pipeline/context/jiraContextBuilder.ts, tests/unit/jira-client.test.ts, tests/unit/jira-graph-traversal.test.ts</files>
  <read_first>src/adapters/jiraClient.ts, .planning/phases/02-context-ingestion-and-confidence-engine/02-RESEARCH.md, docs/tool.md</read_first>
  <action>Implement attachment policy module with allowlisted extractable types (`.txt`, `.md`, `.json`, `.csv`, `.log`, `.xml`) and max-size guard (e.g., `maxAttachmentBytes`). Apply stage-level timeout/retry policy (`maxAttempts`, exponential backoff). In `jiraContextBuilder.ts`, add `completeness` fields (`full`, `partial`, `reasons[]`) and include truncation reason codes (`timeout`, `cap_reached`, `attachment_skipped`).</action>
  <verify>npm run test -- tests/unit/jira-client.test.ts && npm run test -- tests/unit/jira-graph-traversal.test.ts</verify>
  <acceptance_criteria>
    - `jiraAttachmentPolicy.ts` exports allowlist and size-cap checks
    - Jira context output contains `completeness.status` and `completeness.reasons`
    - tests verify timeout/cap path sets `partial` completeness status
  </acceptance_criteria>
  <done>Jira ingestion is secure, bounded, and emits explicit completeness guarantees.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/jira-client.test.ts
- [ ] npm run test -- tests/unit/jira-graph-traversal.test.ts
</verification>

<success_criteria>
- Local-tool-only Jira ingestion contract is implemented with redaction-safe failures.
- Mandatory deep-fetch rules are encoded for task/subtask/epic/linked relationships.
- Traversal outputs include caps, truncation, and completeness metadata.
</success_criteria>

<output>
After completion, create .planning/phases/02-context-ingestion-and-confidence-engine/02-02-SUMMARY.md.
</output>
