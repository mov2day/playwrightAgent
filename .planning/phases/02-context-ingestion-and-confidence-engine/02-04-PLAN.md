---
phase: 02-context-ingestion-and-confidence-engine
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/pipeline/confidence/confidenceContracts.ts
  - src/pipeline/confidence/confidenceEngine.ts
  - src/pipeline/confidence/explainability.ts
  - src/pipeline/orchestrator.ts
  - src/participant/handler.ts
  - src/adapters/eventSink.ts
  - tests/unit/confidence-engine.test.ts
  - tests/integration/confidence-gate-flow.test.ts
autonomous: true
requirements: [CONF-03, CONF-04, CONF-05, CONF-06]
user_setup: []
must_haves:
  truths:
    - Confidence score composition is explainable and profile-versioned.
    - Gate policy is fixed to `<40 reject`, `40-70 approval`, `>70 continue`.
    - Gate UX must expose quick `Continue`/`Cancel` actions and support free-text comments.
  artifacts:
    - src/pipeline/confidence/confidenceEngine.ts computes component and final scores.
    - src/pipeline/confidence/explainability.ts emits sanitized evidence references.
    - tests/integration/confidence-gate-flow.test.ts validates gate branch behavior and action payload.
  key_links:
    - confidenceEngine consumes outputs from repoAnalyzer, jiraContextBuilder, and confluenceContextBuilder.
    - participant handler and orchestrator use gate decision payload to transition safely.
---

<objective>
Implement confidence engine, explainability payload, and gate decision integration.

Purpose: Turn context quality signals into deterministic gate outcomes with human-review controls and audit-ready explanations.
Output: Confidence contracts/engine, sanitizer, participant/orchestrator integration, and tests.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-RESEARCH.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-01-PLAN.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-02-PLAN.md
@.planning/phases/02-context-ingestion-and-confidence-engine/02-03-PLAN.md
@src/pipeline/orchestrator.ts
@src/participant/handler.ts
</context>

<threat_model>
<trust_boundaries>
- Scoring boundary: component inputs influence gate outcomes and must be reproducible.
- Presentation boundary: explainability payload is shown to users and persisted for audit.
- Action boundary: gate actions alter orchestration state and must remain controlled.
</trust_boundaries>

<threat_register>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | score composition | mitigate | versioned weight profiles + deterministic normalization |
| T-02-11 | Information disclosure | explainability payload | mitigate | sanitizer strips token/header/secret-like values before emission |
| T-02-12 | Elevation of privilege | gate transition actions | mitigate | only `continue`/`cancel` quick actions mapped for this gate band; free-text is advisory context |
</threat_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Define confidence contracts, weight profiles, and deterministic scoring engine</name>
  <files>src/pipeline/confidence/confidenceContracts.ts, src/pipeline/confidence/confidenceEngine.ts, tests/unit/confidence-engine.test.ts</files>
  <read_first>.planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md, src/pipeline/repoAnalysis/contracts.ts, src/pipeline/context/jiraContextBuilder.ts</read_first>
  <action>Create confidence contracts for component scores (`repo`, `jira`, `confluence`, `user_context`), weight profile (`profileId`, `version`, `weights`), and final decision payload. Implement `computeConfidenceDecision(input, profile)` with fixed thresholds exactly: `<40 => reject`, `>=40 && <=70 => approval_required`, `>70 => continue`. Add tests that assert boundary values `39.99`, `40`, `70`, and `70.01` route to correct gates.
</action>
  <verify>npm run test -- tests/unit/confidence-engine.test.ts</verify>
  <acceptance_criteria>
    - `confidenceContracts.ts` defines interface `ConfidenceWeightProfile`
    - `confidenceEngine.ts` exports `computeConfidenceDecision`
    - unit tests include explicit threshold boundary assertions for 40 and 70
  </acceptance_criteria>
  <done>Confidence computation is deterministic, profile-based, and threshold-correct.</done>
</task>

<task type="auto">
  <name>Task 2: Implement explainability and sanitized evidence payload generation</name>
  <files>src/pipeline/confidence/explainability.ts, src/adapters/eventSink.ts, tests/unit/confidence-engine.test.ts</files>
  <read_first>.planning/phases/02-context-ingestion-and-confidence-engine/02-RESEARCH.md, src/pipeline/events.ts, src/adapters/eventSink.ts</read_first>
  <action>Implement `buildConfidenceExplainability(decisionInput)` to emit component breakdown, threshold comparisons, and sanitized evidence refs (`issueKey`, `pageId`, `findingId`, bounded snippet). Implement sanitizer to remove values matching credential/token/header patterns (`Authorization`, `Bearer`, `apiKey`, `token`, `secret`). Extend event payload typing to carry `confidenceProfileId` and `decisionGate` for audit trace.</action>
  <verify>npm run test -- tests/unit/confidence-engine.test.ts</verify>
  <acceptance_criteria>
    - `explainability.ts` exports `buildConfidenceExplainability`
    - sanitizer removes keys containing `token`, `secret`, or `Authorization`
    - event sink payload type includes fields `confidenceProfileId` and `decisionGate`
  </acceptance_criteria>
  <done>Gate explanations are secure and audit-ready.</done>
</task>

<task type="auto">
  <name>Task 3: Wire gate decision to participant/orchestrator with required actions and free-text loop support</name>
  <files>src/pipeline/orchestrator.ts, src/participant/handler.ts, tests/integration/confidence-gate-flow.test.ts</files>
  <read_first>src/pipeline/confidence/confidenceEngine.ts, src/participant/actions.ts, .planning/phases/02-context-ingestion-and-confidence-engine/02-CONTEXT.md</read_first>
  <action>Integrate confidence decision stage into request flow before plan generation gate. For `approval_required` results, emit response payload with quick actions `continue` and `cancel` only, plus `acceptsFreeText: true` and `freeTextPurpose: additional_context_or_instruction`. Ensure free-text messages append context and trigger recomputation path rather than bypassing gate transitions. Add integration tests for reject, approval-required, and auto-continue branches.</action>
  <verify>npm run test:integration -- tests/integration/confidence-gate-flow.test.ts</verify>
  <acceptance_criteria>
    - approval-required payload includes exactly quick actions `continue` and `cancel`
    - payload includes `acceptsFreeText: true`
    - integration tests include named cases `reject under 40`, `gate between 40 and 70`, `continue above 70`
  </acceptance_criteria>
  <done>Confidence gate behavior is enforced in orchestration with required user controls.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/confidence-engine.test.ts
- [ ] npm run test:integration -- tests/integration/confidence-gate-flow.test.ts
</verification>

<success_criteria>
- Confidence scoring is explainable, deterministic, and profile-versioned.
- Fixed threshold policy is enforced exactly.
- Gate interactions support Continue/Cancel quick actions and free-text context loops.
</success_criteria>

<output>
After completion, create .planning/phases/02-context-ingestion-and-confidence-engine/02-04-SUMMARY.md.
</output>
