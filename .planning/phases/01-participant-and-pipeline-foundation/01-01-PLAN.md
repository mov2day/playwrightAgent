---
phase: 01-participant-and-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/extension.ts
  - src/participant/handler.ts
  - src/participant/actions.ts
  - src/adapters/eventSink.ts
  - tests/integration/activation.test.ts
autonomous: true
requirements: [PART-01]
user_setup: []
must_haves:
  truths:
    - VS Code extension activation path registers PlaywrightAgent participant exactly once.
    - Participant handler can receive chat requests and delegate to pipeline boundary.
  artifacts:
    - package.json defines lint, typecheck, test, and test:integration scripts.
    - src/extension.ts exports activate/deactivate with participant registration.
    - tests/integration/activation.test.ts validates registration wiring.
  key_links:
    - extension activate function invokes participant handler wiring via src/participant/handler.ts.
    - participant action mapping in src/participant/actions.ts is accessible from handler.
---

<objective>
Establish the extension scaffold and participant registration baseline for `@PlaywrightAgent`.

Purpose: Create the minimum production-structured foundation required for all later pipeline stages.
Output: TypeScript extension shell, participant handler wiring, baseline toolchain scripts, and activation integration tests.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-participant-and-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-participant-and-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-participant-and-pipeline-foundation/01-PATTERNS.md
@docs/tool.md
@skills/playwright-skill/run.js
</context>

<threat_model>
<trust_boundaries>
- Chat input boundary: untrusted user text enters slash-command parser.
- Extension host boundary: participant orchestration must remain deterministic and auditable.
- Future local-tooling boundary: foundation code must not embed secrets or direct external credentials.
</trust_boundaries>

<threat_register>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | participant registration | mitigate | Fail-fast config validation and explicit activation checks |
| T-01-02 | Repudiation | activation/event logging | mitigate | Emit structured startup events including request/session metadata |
| T-01-03 | Information disclosure | baseline config handling | mitigate | Centralized env reader with allowlist and no secret echo |
</threat_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Create extension/toolchain scaffold and typed config baseline</name>
  <files>package.json, tsconfig.json, vitest.config.ts, src/adapters/eventSink.ts</files>
  <read_first>.planning/phases/01-participant-and-pipeline-foundation/01-CONTEXT.md, .planning/phases/01-participant-and-pipeline-foundation/01-RESEARCH.md, skills/playwright-skill/lib/helpers.js</read_first>
  <action>Create a TypeScript extension package baseline with scripts (`lint`, `typecheck`, `test`, `test:integration`), strict TS compiler settings, and Vitest configuration. Add a typed event sink contract that requires correlation metadata fields (`requestId`, `stage`, `action`, `timestamp`) and an in-memory sink implementation for phase 1.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `package.json` contains scripts `lint`, `typecheck`, `test`, and `test:integration`
    - `tsconfig.json` enables strict type checking (`"strict": true`)
    - `src/adapters/eventSink.ts` exports interface requiring `requestId` and `stage`
  </acceptance_criteria>
  <done>Scaffold compiles and event sink contract is typed and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement extension activation and participant registration shell</name>
  <files>src/extension.ts, src/participant/handler.ts, src/participant/actions.ts</files>
  <read_first>docs/tool.md, docs/playwright_agent_architecture.html, .planning/phases/01-participant-and-pipeline-foundation/01-CONTEXT.md</read_first>
  <action>Implement extension activate/deactivate entrypoints and register a chat participant named `@PlaywrightAgent` with placeholder routing to handler service. Add typed quick-action constants (`approve`, `reject`, `continue`, `cancel`) and keep business logic delegated outside activation. Ensure one-time registration guard to prevent duplicate handlers.</action>
  <verify>npm run test -- tests/integration/activation.test.ts</verify>
  <acceptance_criteria>
    - `src/extension.ts` exports `activate` and `deactivate`
    - Participant registration path references `@PlaywrightAgent`
    - `src/participant/actions.ts` exports all four action constants: approve, reject, continue, cancel
  </acceptance_criteria>
  <done>Participant is registered through extension activation and routeable to handler.</done>
</task>

<task type="auto">
  <name>Task 3: Add activation/registration integration test coverage</name>
  <files>tests/integration/activation.test.ts</files>
  <read_first>src/extension.ts, src/participant/handler.ts, .planning/ROADMAP.md</read_first>
  <action>Create lightweight integration tests that mock VS Code extension host APIs and verify activation registers participant exactly once, handler wiring is invoked, and quick-action mapping is reachable from registration layer.</action>
  <verify>npm run test:integration -- tests/integration/activation.test.ts</verify>
  <acceptance_criteria>
    - Test asserts registration call count equals `1`
    - Test asserts handler callback is defined and callable
    - Integration test exits with status code `0`
  </acceptance_criteria>
  <done>Participant baseline has automated integration guardrails.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/integration/activation.test.ts
</verification>

<success_criteria>
- All scaffold files exist with compilable TypeScript baseline.
- Participant registration for `@PlaywrightAgent` is implemented and test-covered.
- No secrets are hardcoded or printed in startup/registration path.
</success_criteria>

<output>
After completion, create .planning/phases/01-participant-and-pipeline-foundation/01-01-SUMMARY.md.
</output>
