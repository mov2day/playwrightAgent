---
phase: 01-participant-and-pipeline-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/participant/slashPlanParser.ts
  - src/pipeline/bootstrapContext.ts
  - src/pipeline/contracts.ts
  - src/participant/handler.ts
  - tests/unit/slash-plan-parser.test.ts
  - tests/integration/no-ticket-flow.test.ts
autonomous: true
requirements: [PART-02, PART-03]
user_setup: []
must_haves:
  truths:
    - `/plan <JIRA-ID> [extra-context]` is parsed deterministically with strict ticket format enforcement.
    - `/plan` no-ticket mode immediately triggers guided context collection path.
    - Invalid ticket token is soft-fail and can continue as no-ticket flow.
  artifacts:
    - src/participant/slashPlanParser.ts contains strict ticket parser and mode output union.
    - src/pipeline/bootstrapContext.ts tags user context with source `user_input`.
    - tests/unit/slash-plan-parser.test.ts covers ticket/no-ticket/invalid cases.
  key_links:
    - Participant handler delegates parse responsibility to slashPlanParser.
    - Parsed mode and user context flow into bootstrapContext request payload.
---

<objective>
Implement strict `/plan` command parsing and request-context bootstrap for both ticket and no-ticket modes.

Purpose: Ensure user intent is captured accurately at pipeline entry and prevent ambiguous or unsafe command handling.
Output: Parser module, bootstrap context module, handler wiring updates, and parser/integration test suite.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-participant-and-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-participant-and-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-participant-and-pipeline-foundation/01-PATTERNS.md
@.planning/phases/01-participant-and-pipeline-foundation/01-01-PLAN.md
@docs/tool.md
</context>

<threat_model>
<trust_boundaries>
- Command text boundary: first token can be malicious or malformed.
- Parse-result boundary: downstream pipeline must consume validated shape only.
- User context boundary: trailing context is high-priority and must remain unmodified.
</trust_boundaries>

<threat_register>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | slash parser | mitigate | strict ticket regex, discriminated union parse output, explicit invalid-mode path |
| T-01-05 | Denial of service | parser loops/errors | mitigate | bounded parsing, guard clauses, deterministic fallback path |
| T-01-06 | Repudiation | mode selection trace | mitigate | emit parse-decision event with mode and normalized fields |
</threat_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Build strict slash parser for ticket and no-ticket modes</name>
  <files>src/participant/slashPlanParser.ts, src/pipeline/contracts.ts</files>
  <read_first>.planning/phases/01-participant-and-pipeline-foundation/01-CONTEXT.md, docs/tool.md, skills/playwright-skill/run.js</read_first>
  <action>Implement a pure parser that accepts raw `/plan` input and returns a discriminated union with modes: `ticket`, `no_ticket`, and `invalid_ticket_soft_fail`. Enforce strict ticket token pattern (`[A-Z][A-Z0-9]+-[0-9]+`), capture trailing text as context, and preserve non-matching first token behavior per D-05/D-08.</action>
  <verify>npm run test -- tests/unit/slash-plan-parser.test.ts</verify>
  <acceptance_criteria>
    - `src/participant/slashPlanParser.ts` contains explicit mode literals `ticket`, `no_ticket`, `invalid_ticket_soft_fail`
    - Regex-based strict ticket validation is present
    - Parser function has no direct side effects (no filesystem/process/vscode calls)
  </acceptance_criteria>
  <done>Parser produces deterministic, typed outputs for all supported command forms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement request bootstrap context with source-tagged user input</name>
  <files>src/pipeline/bootstrapContext.ts, src/participant/handler.ts</files>
  <read_first>src/participant/slashPlanParser.ts, .planning/phases/01-participant-and-pipeline-foundation/01-RESEARCH.md</read_first>
  <action>Create bootstrap builder that generates `requestId`, stores command mode, and tags user-provided context as `source: user_input`. Wire handler to invoke bootstrap immediately after parse result and emit parse/bootstrap events via event sink.</action>
  <verify>npm run test -- tests/unit/slash-plan-parser.test.ts</verify>
  <acceptance_criteria>
    - Bootstrap output contains `requestId` and `source: user_input` when context exists
    - Handler imports parser and bootstrap modules rather than duplicating logic
    - Invalid-ticket soft-fail flow preserves user context and switches mode to no-ticket continuation
  </acceptance_criteria>
  <done>Request context payload is canonical and ready for pipeline state management.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit and lightweight integration coverage for parse/bootstrap behavior</name>
  <files>tests/unit/slash-plan-parser.test.ts, tests/integration/no-ticket-flow.test.ts</files>
  <read_first>src/participant/slashPlanParser.ts, src/pipeline/bootstrapContext.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Write parser matrix tests covering valid ticket, ticket+context, no-ticket prompt path, and invalid-token soft-fail. Add integration test asserting `/plan` with no ticket triggers guided prompt and `/plan BADTOKEN context` continues through soft-fail path with context retained.</action>
  <verify>npm run test && npm run test:integration -- tests/integration/no-ticket-flow.test.ts</verify>
  <acceptance_criteria>
    - Unit tests include at least one assertion for each mode branch
    - Integration test validates guided follow-up behavior for no-ticket flow
    - All parse/bootstrap tests pass with exit code `0`
  </acceptance_criteria>
  <done>Command entry behavior for PART-02 and PART-03 is fully test-covered.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/slash-plan-parser.test.ts
- [ ] npm run test:integration -- tests/integration/no-ticket-flow.test.ts
</verification>

<success_criteria>
- Strict ticket parsing and no-ticket path are both implemented and deterministic.
- Invalid ticket format follows soft-fail continuation behavior.
- User context tagging and request bootstrap are covered by tests.
</success_criteria>

<output>
After completion, create .planning/phases/01-participant-and-pipeline-foundation/01-02-SUMMARY.md.
</output>
