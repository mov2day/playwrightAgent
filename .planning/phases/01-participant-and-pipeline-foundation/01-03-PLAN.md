---
phase: 01-participant-and-pipeline-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/pipeline/stateMachine.ts
  - src/pipeline/events.ts
  - src/pipeline/orchestrator.ts
  - src/participant/handler.ts
  - src/ui/planReviewShell.ts
  - tests/unit/pipeline-state-machine.test.ts
  - tests/integration/request-correlation.test.ts
  - tests/smoke/webview-shell.test.ts
autonomous: true
requirements: [PART-04]
user_setup: []
must_haves:
  truths:
    - Pipeline state is request-scoped and transition-safe across gate events.
    - Illegal transitions are blocked with explicit error events.
    - Every stage event includes the originating `requestId`.
  artifacts:
    - src/pipeline/stateMachine.ts defines allowed transition table and guard function.
    - src/pipeline/events.ts exports typed event envelope with requestId.
    - tests/unit/pipeline-state-machine.test.ts validates legal/illegal transitions.
  key_links:
    - handler bootstrapped request context is consumed by orchestrator and state machine.
    - webview shell receives gate state from orchestrator without direct mutation authority.
---

<objective>
Implement deterministic pipeline state management with gate transition guards and request-correlation propagation.

Purpose: Make approval-gated orchestration impossible to bypass and establish the traceability baseline for later audit logging.
Output: FSM/state modules, orchestration wiring, minimal webview shell hook, and unit/integration/smoke tests.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-participant-and-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-participant-and-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-participant-and-pipeline-foundation/01-PATTERNS.md
@.planning/phases/01-participant-and-pipeline-foundation/01-VALIDATION.md
@.planning/phases/01-participant-and-pipeline-foundation/01-01-PLAN.md
@.planning/phases/01-participant-and-pipeline-foundation/01-02-PLAN.md
@docs/tool.md
@docs/playwright_agent_architecture.html
</context>

<threat_model>
<trust_boundaries>
- State transition boundary: only orchestrator can request transitions, machine decides validity.
- Action boundary: chat/webview actions are untrusted intents until validated.
- Event telemetry boundary: correlation data must be complete to prevent audit gaps.
</trust_boundaries>

<threat_register>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Elevation of privilege | gate transition engine | mitigate | central allowed-transition table and deny-by-default behavior |
| T-01-08 | Repudiation | event telemetry | mitigate | request-scoped typed events and mandatory correlation fields |
| T-01-09 | Tampering | webview/chat action handling | mitigate | action normalization + transition validation before state mutation |
</threat_register>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Build explicit FSM with allowed-transition table and guard APIs</name>
  <files>src/pipeline/stateMachine.ts, src/pipeline/events.ts</files>
  <read_first>.planning/phases/01-participant-and-pipeline-foundation/01-CONTEXT.md, .planning/phases/01-participant-and-pipeline-foundation/01-RESEARCH.md, docs/tool.md</read_first>
  <action>Implement canonical state enum and `ALLOWED_TRANSITIONS` map for pipeline steps relevant to phase 1 entry/gate lifecycle. Add transition guard API returning structured result (`ok`, `errorCode`, `from`, `to`) and typed event payload requiring `requestId` on all emitted events.</action>
  <verify>npm run test -- tests/unit/pipeline-state-machine.test.ts</verify>
  <acceptance_criteria>
    - `src/pipeline/stateMachine.ts` defines explicit allowed-transition map
    - Illegal transition path returns explicit error code (no silent fallback)
    - `src/pipeline/events.ts` enforces `requestId` in event types
  </acceptance_criteria>
  <done>FSM can enforce gate-safe transitions with deterministic outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire orchestrator/handler to request-scoped state and action controls</name>
  <files>src/pipeline/orchestrator.ts, src/participant/handler.ts, src/ui/planReviewShell.ts</files>
  <read_first>src/pipeline/stateMachine.ts, src/pipeline/bootstrapContext.ts, .planning/phases/01-participant-and-pipeline-foundation/01-02-PLAN.md</read_first>
  <action>Implement orchestrator that creates per-request in-memory session objects keyed by `requestId`, routes quick actions (`approve`, `reject`, `continue`, `cancel`) through transition guard, and exposes minimal webview shell callback for gate-state rendering without granting mutation authority to UI layer.</action>
  <verify>npm run test:integration -- tests/integration/request-correlation.test.ts</verify>
  <acceptance_criteria>
    - Orchestrator stores state per `requestId` (not global singleton state only)
    - Quick actions route through transition guard API before state change
    - Webview shell receives read-only gate payload and cannot bypass orchestrator
  </acceptance_criteria>
  <done>Handler/orchestrator flow enforces request-scoped, gate-safe behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add FSM, correlation, and webview smoke coverage</name>
  <files>tests/unit/pipeline-state-machine.test.ts, tests/integration/request-correlation.test.ts, tests/smoke/webview-shell.test.ts</files>
  <read_first>src/pipeline/stateMachine.ts, src/pipeline/orchestrator.ts, src/ui/planReviewShell.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create unit tests for legal/illegal transitions and action routing behavior. Add integration tests verifying every emitted event in a request flow includes identical `requestId`. Add smoke test that plan review shell opens and renders stub gate payload.</action>
  <verify>npm run test && npm run test:integration -- tests/integration/request-correlation.test.ts</verify>
  <acceptance_criteria>
    - Unit tests include at least one blocked illegal transition assertion
    - Integration test asserts all events in flow share single requestId
    - Smoke test for `src/ui/planReviewShell.ts` passes
  </acceptance_criteria>
  <done>PART-04 state-management behavior is verifiably enforced.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/pipeline-state-machine.test.ts
- [ ] npm run test:integration -- tests/integration/request-correlation.test.ts
- [ ] npm run test:integration -- tests/smoke/webview-shell.test.ts
</verification>

<success_criteria>
- FSM denies illegal transitions and allows only declared paths.
- Request state is isolated per run and correlated by `requestId`.
- Quick actions and webview shell integrate through orchestration layer safely.
</success_criteria>

<output>
After completion, create .planning/phases/01-participant-and-pipeline-foundation/01-03-SUMMARY.md.
</output>
