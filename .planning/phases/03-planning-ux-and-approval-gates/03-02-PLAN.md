---
phase: 03-planning-ux-and-approval-gates
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - package.json
  - tsconfig.json
  - src/ui/reviewModel.ts
  - src/ui/reviewActions.ts
  - src/ui/reviewApp.tsx
  - src/ui/planReviewShell.ts
  - tests/smoke/webview-shell.test.ts
  - tests/smoke/webview-review-tabs.test.ts
  - tests/unit/review-actions.test.ts
autonomous: true
requirements: [PLAN-03, PLAN-04, PLAN-05]
user_setup: []
must_haves:
  truths:
    - Webview renders grouped review tabs (`All`, requirement/AC groups, `Rejected`) with scenario counts.
    - User can approve/reject each scenario and execute bulk approve/reject controls.
    - Review UI includes both per-scenario and global comments plus explicit revise CTA.
  artifacts:
    - src/ui/reviewModel.ts defines typed webview view model with grouped tabs and comment targets.
    - src/ui/reviewActions.ts defines normalized UI action envelope including bulk and revise actions.
    - src/ui/reviewApp.tsx implements React + MUI review surface.
    - src/ui/planReviewShell.ts hosts webview app payload and bridge messages.
  key_links:
    - orchestrator snapshot payload -> reviewModel -> React tab rendering.
    - scenario action click -> reviewActions envelope -> extension host bridge dispatch.
---

<objective>
Implement professional React + MUI review surface for plan approval in VS Code webview.

Purpose: Satisfy tabbed grouped UX and approval controls while preserving one normalized action contract for sync.
Output: Typed review model/actions, React+MUI webview app, shell bridge integration, and UI smoke/unit coverage.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-planning-ux-and-approval-gates/03-CONTEXT.md
@.planning/phases/03-planning-ux-and-approval-gates/03-RESEARCH.md
@.planning/phases/03-planning-ux-and-approval-gates/03-PATTERNS.md
@.planning/phases/03-planning-ux-and-approval-gates/03-UI-SPEC.md
@.planning/phases/03-planning-ux-and-approval-gates/03-01-PLAN.md
@src/ui/planReviewShell.ts
@src/participant/actions.ts

<interfaces>
From `src/ui/planReviewShell.ts`:
```ts
export interface PlanReviewShellPayload {
  requestId: string;
  state: PipelineState;
  summary: string;
  actions: readonly QuickAction[];
}
```

From `src/participant/actions.ts`:
```ts
export const QUICK_ACTIONS = ['approve', 'reject', 'continue', 'cancel'] as const;
export type QuickAction = (typeof QUICK_ACTIONS)[number];
```

From plan contracts introduced in 03-01:
```ts
export interface PlanReviewBundle {
  flatScenarios: ScenarioPlanRecord[];
  groupIndexes: ScenarioGroupingIndexes;
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension host -> webview payload | Untrusted/escaped content rendered in browser context |
| webview click/comment -> extension action message | User input crosses into approval mutation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | S | webview message bridge | mitigate | Add explicit action `type` union allowlist in `reviewActions.ts`; reject unknown event types |
| T-03-05 | XSS | `reviewApp.tsx` and `planReviewShell.ts` | mitigate | Render all scenario/comment text through React escaped nodes; keep host-side `escapeHtml` for shell attributes |
| T-03-06 | D | bulk action controls | mitigate | Enforce pending-only default for bulk actions with explicit `mode: 'pending_only' | 'force_override'` field |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Define webview review model and action envelopes for grouped tabs and comments</name>
  <files>src/ui/reviewModel.ts, src/ui/reviewActions.ts, tests/unit/review-actions.test.ts</files>
  <read_first>.planning/phases/03-planning-ux-and-approval-gates/03-CONTEXT.md, .planning/phases/03-planning-ux-and-approval-gates/03-UI-SPEC.md, src/participant/actions.ts, src/ui/planReviewShell.ts, src/pipeline/confidence/explainability.ts</read_first>
  <acceptance_criteria>
    - `rg \"tabId: 'all' \\| 'by_requirement' \\| 'by_acceptance_criteria' \\| 'by_functionality' \\| 'rejected'\" src/ui/reviewModel.ts` returns one match (D-05)
    - `rg \"target: 'scenario' \\| 'global'\" src/ui/reviewModel.ts` returns one match (D-07)
    - `rg \"classification: 'clarification' \\| 'constraint' \\| 'bug' \\| 'new_context'\" src/ui/reviewModel.ts` returns one match (D-15)
    - `rg \"sanitize\" src/ui/reviewModel.ts` returns sanitization helper(s) for rendered review text aligned with explainability transform pattern
    - `rg \"type: 'scenario.approve'\" src/ui/reviewActions.ts` and `rg \"type: 'bulk.reject'\" src/ui/reviewActions.ts` both return matches (D-06)
  </acceptance_criteria>
  <action>Create `ReviewViewModel` and `ReviewTabView` types encoding tabs `all`, `by_requirement`, `by_acceptance_criteria`, `by_functionality`, `rejected` (D-05). Mirror the model sanitization approach from `src/pipeline/confidence/explainability.ts` by adding explicit review-text sanitizers for scenario/global comment and narrative fields before they enter the webview view model. Define `ReviewActionEnvelope` union with concrete action IDs: `scenario.approve`, `scenario.reject`, `scenario.revise`, `bulk.approve`, `bulk.reject`, `comment.add`, `session.continue`, `session.cancel`. Include `bulk` payload field `mode: 'pending_only' | 'force_override'` (D-12) and include `revise` action for webview CTA (D-17). Add unit tests that fail on unknown action type and verify pending-only default mode.</action>
  <verify>
    <automated>npm run test -- tests/unit/review-actions.test.ts</automated>
  </verify>
  <done>Review model/action contracts represent all required tabs, comments, per-item, bulk, and revise actions.</done>
</task>

<task type="auto">
  <name>Task 2: Build React + MUI review application and wire it into webview shell host</name>
  <files>package.json, tsconfig.json, src/ui/reviewApp.tsx, src/ui/planReviewShell.ts, tests/smoke/webview-shell.test.ts</files>
  <read_first>package.json, tsconfig.json, .planning/phases/03-planning-ux-and-approval-gates/03-UI-SPEC.md, src/ui/planReviewShell.ts, .planning/phases/03-planning-ux-and-approval-gates/03-PATTERNS.md</read_first>
  <acceptance_criteria>
    - `rg '\"react\"' package.json` and `rg '\"@mui/material\"' package.json` both return dependency entries (D-08)
    - `rg '\"jsx\": \"react-jsx\"' tsconfig.json` returns one match
    - `rg \"Tabs|Tab|AppBar|Chip|Button\" src/ui/reviewApp.tsx` returns MUI component usage (D-08, D-06)
    - `rg \"#F7F5EE|#E6E1D2|#0E7490|#B42318\" src/ui/reviewApp.tsx` returns UI-SPEC color tokens (D-09)
  </acceptance_criteria>
  <action>Install UI runtime dependencies in `package.json`: `react`, `react-dom`, `@mui/material`, `@mui/icons-material`, `@emotion/react`, `@emotion/styled`. Update `tsconfig.json` with `"jsx": "react-jsx"` and include `src/**/*.tsx`. Implement `reviewApp.tsx` using MUI `Tabs` + `Tab` + scenario cards + sticky bottom bulk action bar with minimum button height `44px` and design token palette from `03-UI-SPEC.md` (D-09). Update `planReviewShell.ts` to host React root container and pass serialized `ReviewViewModel` payload to webview script bootstrap (D-08).</action>
  <verify>
    <automated>npm run test -- tests/smoke/webview-shell.test.ts</automated>
  </verify>
  <done>Webview host renders React+MUI review root and honors visual contract tokens and layout rules.</done>
</task>

<task type="auto">
  <name>Task 3: Implement tabbed grouped scenario review interactions with per-item and bulk controls</name>
  <files>src/ui/reviewApp.tsx, src/ui/reviewActions.ts, tests/smoke/webview-review-tabs.test.ts, tests/smoke/webview-shell.test.ts</files>
  <read_first>.planning/phases/03-planning-ux-and-approval-gates/03-CONTEXT.md, .planning/phases/03-planning-ux-and-approval-gates/03-UI-SPEC.md, src/ui/reviewModel.ts, src/ui/reviewActions.ts</read_first>
  <acceptance_criteria>
    - `rg \"data-tab=\\\"rejected\\\"\" src/ui/reviewApp.tsx` returns tab marker (D-05)
    - `rg \"scenario.approve|scenario.reject|scenario.revise\" src/ui/reviewApp.tsx` returns three action dispatches (D-06, D-17)
    - `rg \"bulk.approve|bulk.reject\" src/ui/reviewApp.tsx` returns sticky bulk action dispatches (D-06)
    - `rg \"target: 'global'|target: 'scenario'\" src/ui/reviewApp.tsx` returns both comment entry points (D-07)
    - `npm run test -- tests/smoke/webview-review-tabs.test.ts` exits 0
  </acceptance_criteria>
  <action>Render tabs for `All`, requirement groups, acceptance-criteria groups, functionality groups, and `Rejected` view (D-05). Add per-scenario buttons `Approve`, `Reject`, `Revise` that dispatch normalized action envelope values (D-06, D-17). Add sticky bulk bar with `Approve Pending` and `Reject Pending` actions using `mode: 'pending_only'` plus secondary force override action for explicit overwrite (D-12). Provide both scenario-level and global comment editors that dispatch `comment.add` with target and classification fields (D-07, D-15).</action>
  <verify>
    <automated>npm run test -- tests/smoke/webview-review-tabs.test.ts tests/smoke/webview-shell.test.ts</automated>
  </verify>
  <done>Webview review UX covers grouped tabs, per-item controls, bulk controls, revise CTA, and comment entry points.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/review-actions.test.ts
- [ ] npm run test -- tests/smoke/webview-shell.test.ts tests/smoke/webview-review-tabs.test.ts
</verification>

<success_criteria>
- PLAN-03 tabbed grouped review surface is delivered in React + MUI.
- PLAN-04 per-scenario approve/reject and revise controls are available.
- PLAN-05 pending-safe bulk approve/reject controls are available with explicit override mode.
</success_criteria>

<output>
After completion, create `.planning/phases/03-planning-ux-and-approval-gates/03-02-SUMMARY.md`
</output>
