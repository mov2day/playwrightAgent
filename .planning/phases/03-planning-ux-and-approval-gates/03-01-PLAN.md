---
phase: 03-planning-ux-and-approval-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/planning/planContracts.ts
  - src/pipeline/planning/scenarioMapper.ts
  - src/pipeline/planning/scenarioGrouping.ts
  - src/pipeline/planning/planSummary.ts
  - src/participant/handler.ts
  - tests/unit/plan-contracts.test.ts
  - tests/unit/scenario-grouping.test.ts
  - tests/integration/plan-chat-summary.test.ts
autonomous: true
requirements: [PLAN-01, PLAN-02]
user_setup: []
must_haves:
  truths:
    - Chat plan output includes scenario name, scope, assertion intent, risk, and requirement/AC mapping.
    - Scenario plan data can render both flat list and grouped views without recomputing group keys per render.
    - Plan contract preserves requirement traceability per scenario for later approval and exclusion logic.
  artifacts:
    - src/pipeline/planning/planContracts.ts defines scenario plan and approval-ready DTOs.
    - src/pipeline/planning/scenarioMapper.ts maps requirement context into scenario plan entities.
    - src/pipeline/planning/scenarioGrouping.ts precomputes requirement/AC/functionality group indexes.
    - src/pipeline/planning/planSummary.ts renders structured chat summary text.
  key_links:
    - scenarioMapper output -> scenarioGrouping indexes -> planSummary renderer -> participant response payload.
    - primaryRequirementId + acceptanceCriteriaIds fields remain consistent across mapper, grouping, and summary output.
---

<objective>
Define Phase 3 plan contracts and deterministic plan-to-chat rendering pipeline.

Purpose: Lock requirement-mapped scenario data shape first so webview and orchestrator can consume one stable contract.
Output: New planning DTOs, mapper/grouping utilities, chat summary formatter, and contract-focused tests.
</objective>

<execution_context>
@/Users/muthu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/muthu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-planning-ux-and-approval-gates/03-CONTEXT.md
@.planning/phases/03-planning-ux-and-approval-gates/03-RESEARCH.md
@.planning/phases/03-planning-ux-and-approval-gates/03-PATTERNS.md
@.planning/phases/03-planning-ux-and-approval-gates/03-UI-SPEC.md
@src/pipeline/contracts.ts
@src/participant/handler.ts
@src/ui/planReviewShell.ts

<interfaces>
From `src/pipeline/contracts.ts`:
```ts
export interface RequestContext {
  requestId: string;
  mode: PlanMode;
  ticketId?: string;
  userContext?: UserInputContext;
  warnings: string[];
  createdAt: string;
  stage: 'context_bootstrapped';
}
```

From `src/participant/handler.ts`:
```ts
export interface PlanCommandResponse {
  requestId: string;
  mode: PlanMode;
  state?: PipelineState;
  message: string;
  availableActions: readonly QuickAction[];
  confidenceScore: number;
  decisionGate: ConfidenceGate;
  acceptsFreeText: boolean;
}
```

From `src/participant/actions.ts`:
```ts
export const QUICK_ACTIONS = ['approve', 'reject', 'continue', 'cancel'] as const;
export type QuickAction = (typeof QUICK_ACTIONS)[number];
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning mapper -> participant response | Untrusted Jira/user text becomes displayed plan content |
| grouped indexes -> downstream UI state | Incorrect mapping can misroute approval decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `scenarioMapper.ts` | mitigate | Validate required fields (`scenarioId`, `primaryRequirementId`, `acceptanceCriteriaIds`) before emitting mapped scenarios |
| T-03-02 | I | `planSummary.ts` | mitigate | Escape/sanitize summary fields before chat/webview rendering, never include credential-like fields |
| T-03-03 | R | plan trace fields | mitigate | Include `primaryRequirementId` + `acceptanceCriteriaIds[]` + `sourceEvidenceIds[]` on every scenario record for replay |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Create canonical scenario plan contracts per locked data decisions</name>
  <files>src/pipeline/planning/planContracts.ts, tests/unit/plan-contracts.test.ts</files>
  <read_first>.planning/phases/03-planning-ux-and-approval-gates/03-CONTEXT.md, .planning/REQUIREMENTS.md, src/pipeline/contracts.ts, src/pipeline/confidence/confidenceContracts.ts</read_first>
  <acceptance_criteria>
    - `rg "primaryRequirementId: string" src/pipeline/planning/planContracts.ts` returns one match (D-01)
    - `rg "acceptanceCriteriaIds: string\\[\\]" src/pipeline/planning/planContracts.ts` returns one match (D-01)
    - `rg "riskLevel: 'low' \\| 'medium' \\| 'high'" src/pipeline/planning/planContracts.ts` returns one match (D-02)
    - `rg "assertionIntentSummary: string" src/pipeline/planning/planContracts.ts` returns one match (D-03)
    - `rg "revisionReason: string\\[\\]" src/pipeline/planning/planContracts.ts` returns one match (D-11)
  </acceptance_criteria>
  <action>Create `ScenarioPlanRecord`, `ScenarioGroupingIndexes`, and `PlanReviewBundle` types with exact fields: `scenarioId`, `scenarioName`, `scope`, `assertionIntentSummary`, `primaryRequirementId`, `acceptanceCriteriaIds`, `riskLevel`, `riskReason`, `mitigation?`, `sourceEvidenceIds[]`, `approvalState`, `revisionReason[]`, and `commentRefs[]`. Add `PlanReviewBundle` fields `flatScenarios` and `groupIndexes` for precomputed group usage (D-04). Add unit tests asserting required keys, risk enum values, and approval state union `pending|approved|rejected|needs_revision` (D-11).</action>
  <verify>
    <automated>npm run test -- tests/unit/plan-contracts.test.ts</automated>
  </verify>
  <done>Plan contract file exposes stable scenario DTOs matching D-01, D-02, D-03, D-04, and D-11 with unit coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic scenario mapping and precomputed grouping indexes</name>
  <files>src/pipeline/planning/scenarioMapper.ts, src/pipeline/planning/scenarioGrouping.ts, tests/unit/scenario-grouping.test.ts</files>
  <read_first>.planning/phases/03-planning-ux-and-approval-gates/03-CONTEXT.md, .planning/phases/03-planning-ux-and-approval-gates/03-PATTERNS.md, src/pipeline/context/confluenceQueryBuilder.ts, src/pipeline/context/jiraContextBuilder.ts</read_first>
  <acceptance_criteria>
    - `rg "buildScenarioPlan\\(" src/pipeline/planning/scenarioMapper.ts` returns exported function
    - `rg "byRequirementId" src/pipeline/planning/scenarioGrouping.ts` returns exported grouped index key (D-04)
    - `rg "byAcceptanceCriteriaId" src/pipeline/planning/scenarioGrouping.ts` returns exported grouped index key (D-04)
    - `rg "byFunctionality" src/pipeline/planning/scenarioGrouping.ts` returns exported grouped index key (D-04)
    - `npm run test -- tests/unit/scenario-grouping.test.ts` exits 0 and includes deterministic sort assertion
  </acceptance_criteria>
  <action>Implement `buildScenarioPlan(requirementContext, options)` in `scenarioMapper.ts` that maps each candidate scenario into `ScenarioPlanRecord` with normalized `scenarioId` format `scn_<requirementId>_<index>`. Implement `buildScenarioGroupingIndexes(records)` in `scenarioGrouping.ts` that outputs `byRequirementId`, `byAcceptanceCriteriaId`, `byFunctionality`, and `rejectedScenarioIds` indexes from `flatScenarios` with stable sort by `scenarioName`. Persist indexes on bundle creation so tabs do not recompute groups at render time (D-04).</action>
  <verify>
    <automated>npm run test -- tests/unit/scenario-grouping.test.ts</automated>
  </verify>
  <done>Mapping and grouping utilities emit stable flat + grouped structures for downstream chat and webview renderers.</done>
</task>

<task type="auto">
  <name>Task 3: Add structured chat plan summary renderer and participant payload wiring</name>
  <files>src/pipeline/planning/planSummary.ts, src/participant/handler.ts, tests/integration/plan-chat-summary.test.ts</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, src/participant/handler.ts, src/participant/actions.ts, src/pipeline/planning/planContracts.ts</read_first>
  <acceptance_criteria>
    - `rg "export function formatPlanChatSummary" src/pipeline/planning/planSummary.ts` returns one match
    - `rg "planSummary:" src/participant/handler.ts` returns response payload field for structured summary (PLAN-02)
    - `rg "assertionIntentSummary" src/pipeline/planning/planSummary.ts` returns table row rendering field (PLAN-01)
    - `npm run test:integration -- tests/integration/plan-chat-summary.test.ts` exits 0
  </acceptance_criteria>
  <action>Create `formatPlanChatSummary(bundle: PlanReviewBundle): string` that renders markdown sections in this order: `Scenario`, `Scope`, `Risk`, `Assertion Intent`, `Requirement`, `Acceptance Criteria IDs`. Extend `PlanCommandResponse` with `planSummary` and `planScenarios` optional fields, and wire `handlePlanCommand` to include structured summary when plan bundle exists (PLAN-02). Ensure row values read from `primaryRequirementId` + `acceptanceCriteriaIds[]` + risk fields from contract (PLAN-01, D-01, D-02, D-03).</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/plan-chat-summary.test.ts</automated>
  </verify>
  <done>Chat response can carry structured requirement-mapped scenario summary without breaking existing confidence gate behavior.</done>
</task>
</tasks>

<verification>
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run test -- tests/unit/plan-contracts.test.ts tests/unit/scenario-grouping.test.ts
- [ ] npm run test:integration -- tests/integration/plan-chat-summary.test.ts
</verification>

<success_criteria>
- PLAN-01 fields are represented in contracts and summary output.
- PLAN-02 structured textual summary is available in participant response payload.
- Flat scenarios and grouped indexes are both persisted per D-04.
</success_criteria>

<output>
After completion, create `.planning/phases/03-planning-ux-and-approval-gates/03-01-SUMMARY.md`
</output>
